# Activity Log (역시간순 — 최신 위)

## 2026-08-02 · 축척 전환에 설명을 붙이다 + 자기비판(5.15)
- 오너: **"중간에 그리드가 나오는데 왜 나오는 건가요?"** → 5.14에서 넣은 **축척 전환**이다. 실측하니 질량 **8,000~10,000**(camDist 171) 근처에서 26 → 260으로 바뀌며 한 번 반짝인다.
  - **말없이 반짝이기만 하니 '왜 갑자기 격자가 나오지?'로 읽혔다.** 이 전환이 곧 성장 신호이므로 그렇게 말해 준다 — `◇ 축척 ×10 · 자를 넘어섰다 — 이제 격자 한 칸이 260 단위다`(양 언어, 우선순위 3).
  - 교훈: **보이는 변화엔 설명이 따라야 한다.** 링 색·펄서 빔·HOLD 동사·축척 전환 — 이번 세션에 같은 실수를 네 번 했다.
- 오너: **"후반부에 여전히 블랙홀만 먹는 것 말고 뭐가 달라진 건가요?"**(3회째 같은 리포트)
  - **캐시 배제**: 라이브 `cache-control: max-age=600`, 캐시버스터 없이 받아도 최신 버전이 나온다 — 오너는 새 빌드를 받고 있다. 이 가설은 기각.
  - **자기비판**: 5.13의 포획(HOLD)은 내가 '두 번째 동사'라 불렀지만, 실제로는 **같은 동사에 수식키를 붙인 것**이다. 여전히 천체에 다가가 닿는 행동이고, 보상도 같은 종류(질량·점수)다. 손맛이 바뀌지 않으니 "달라진 게 없다"는 리포트가 정확하다.
  - **진짜 빈 곳(가설)**: 이 게임엔 **공간 판단이 없다.** `topUp()`이 플레이어 주위에 균일하게 스폰하므로 **모든 방향이 동일**하고, '어디로 갈까'라는 결정이 존재하지 않는다. 그래서 이벤트·지형·목적지를 넣어도 스쳐 지나가는 점에 그쳤다. 필드가 비균일해져야(밀집 지대·공백·중심 방향) 위치가 의미를 갖고, 나침반·그리드도 그제야 쓸모가 생긴다.
  - 다음 수를 임의로 만들지 않고 오너에게 이 진단을 제시했다.
- 전 회귀 **458/458**(29스위트).

## 2026-08-02 · 새 동사를 못 본 이유 · 그리드 축척 · 배너 한국어(5.14)
- 오너 리포트 3건. 셋 다 실측으로 원인 확정.
- **① "블랙홀이 된 후 먹는 것 외에 차이가 없다 · 뭐가 달라진지 모르겠다"** — 5.13에서 넣은 포획 동사를 **볼 수가 없었다.** `onboardTick`의 `if(onboarded&&S.hintIdx>0)return` 때문에, `acc_onboard`가 오래전에 설정된 재방문 플레이어는 **HOLD 안내를 본 적도 앞으로 볼 일도 없었다.** 링 색 안내가 안 보였던 것과 같은 함정이다.
  - **고침**: 새 동사는 '생애 첫 판'이 아니라 **'쓸 수 있게 된 순간'**에 가르친다 — 블랙홀 전이 직후 `onboarded`와 무관하게 매 판 배너. 시작 카드 조작 목록에도 `C · hold · bank into orbit` 추가(두 경로).
  - 부수 버그: 그 배너를 `setTimeout(1500)`으로 띄워, 그 사이 죽으면 **결과 화면에 잔류**했다 → `if(S.alive)` 가드.
- **② "어느 순간부터 그리드가 끊긴다 · 효용을 모르겠다"** — 눈금이 26 고정이라 고질량(camDist 535)에서 셀이 화면에 너무 촘촘해 선이 가늘어지고 불규칙해졌다(밉맵을 끈 상태라 앨리어싱).
  - **고침**: 지도처럼 **10배씩 축척을 전환**한다(camDist 기준, 26 → 260 → …). 전환 순간 한 번 반짝여 의도된 일로 읽히게 했다.
  - **부수 효과가 본질이다** — 축척이 바뀌는 순간 세상이 한 단계 물러나며 '내가 그만큼 커졌다'가 눈에 보인다. 고정 눈금은 그걸 못 보여줬고, 그래서 효용이 안 느껴졌다.
- **③ "한국어를 선택해도 전체적으로 반영돼 있지 않다"** — 맞다. 5.4의 다국어는 **진화 카드 12줄뿐**이었다.
  - **고침**: 배너·힌트 **46문자열** 사전을 추가하고 `banner()` 안에서 조회한다(호출부 20여 곳을 뜯지 않아 안전·되돌리기 쉬움). 회귀 2건으로 양방향 고정.
  - **남은 것**: 코덱스 팩트 **39개**는 아직 영문이다. 분량이 커서 다음 차례로 남긴다 — 오너에게 명시했다.
- **UI 회귀 2건 자체 적발**: DISK 행·C 행이 늘면서 낮은 화면에서 넘쳤다(`ob-h⨯track` 390px) → 400px 단계 압축 추가.
- 전 회귀 **456/456**(29스위트).

## 2026-08-02 · 두 번째 동사 — 삼킬까, 쌓을까(5.13)
- 오너: **"재미있는 방향으로 진행하세요. 목적이 비용 감축에 맞춰지지 않고 최선의 결과로 이어지도록 설계해야죠."**
- **추천을 뒤집었다.** 5.12 시안에서 A(사냥)+B(펜로즈)를 권했는데, 그 근거에 "밸런스 위험 낮음 · 기존 시스템 재사용"이 섞여 있었다 — **비용 논리였다.** 결과 기준으로 다시 보면:
  - A(사냥)는 *목표*를 바꿀 뿐 하는 행동은 여전히 '먹기'다.
  - **C(원반 짓기)만이 매 순간의 결정을 바꾼다** — 지금은 천체를 보면 답이 하나뿐이지만, 쌓기가 생기면 매번 저울질이 생긴다. 가장 크고 위험해서 내가 3순위로 밀었던 것이 결과 기준으론 1순위였다.
- **구현 — 강착 원반(포획)**: `C` 키(또는 `◍ HOLD` 버튼)를 누르고 있는 동안, 먹는 대신 **궤도에 붙잡는다**.
  - 궤도 질량은 초당 55%씩 안쪽으로 내려와 내 질량이 된다(각운동량을 버려야 내려간다는 실제 원리).
  - **저울질이 성립한다(실측)**: 같은 먹이 4개 기준 — 질량 **+278 → +426(×1.53)**, 점수 **21,095 → 50,883(×2.41)**. 대신 궤도가 다 내려오는 데 16초가 더 걸리고, 그동안 **내 질량이 아니며**, 궤도 질량만큼 **느려진다**(실측 이동거리 80.4 → 63.6).
  - 슬롯은 티어당 늘어난다(블랙홀 3 → 극대질량 7). 원시별·항성 단계엔 없는 동사다.
  - **제트가 원반을 쏜다** — 쌓아 둔 것이 탄약이 된다. 실제로 제트는 안쪽 원반에서 쏘아 올려진다.
- **자체 적발 3건**: ① 포획한 천체에 `eaten` 플래그를 썼더니 정리 경로가 다음 프레임에 메시를 지워 궤도가 비었다 → `objects`에서만 빼고 씬에는 남기는 소유권 이전으로. ② 키다운 핸들러를 열고 안 닫아 페이지가 구문 오류로 죽었다. ③ HUD에 DISK 행이 늘면서 낮은 화면(360·393px)에서 하단 바를 침범했다(`pval⨯track`·`cval⨯actDash`) → 430px·380px 두 단계 압축, 겹침 6→0.
- 코덱스 `ACCRETION DISK` 추가(38→39). 힌트도 조건부로 추가 — 블랙홀이 되면 알려준다.
- 회귀 **+6건**(escalation 18→24): 블랙홀 전용 동사인가 · 실제로 궤도에 붙잡히는가 · 쌓기가 더 버는가(질량·점수) · 결국 다 내려오는가 · 실으면 느려지는가.
- 전 회귀 **452/452**(29스위트).

## 2026-08-02 · 최종 형태가 시공간을 휜다 + 중후반 시안(5.12)
- 오너: **"블랙홀 단계 이후 후반까지 행성 먹는 것 외에 별다른 게 없어 매우 무료하다"** · **"콤보가 일정 수준을 넘어도 시공간을 왜곡하는 최종 형태가 안 보인다"** · **"시안이 있다면 html로 보여주고 진행하라"**.
- **최종 형태 문제 — 원인 확정**: 완전체(EXTREMAL KERR)는 `paintDisk(흰색)`과 광자 고리 ×1.16만 바꿨고 **렌징을 전혀 건드리지 않았다.** 고질량에선 원반이 이미 하얗게 포화돼 있어(5.7에서 실측한 그 현상) 사실상 아무 변화가 없었다.
  - **고침**: 발동 중 `uStrength`에 최대 ×2.35, `uEH`에 ×1.45를 곱한다(실측 0.145 → 0.356, **×2.45**). 진입·종료에 1.2초 램프.
  - **에르고구면 고리** 신설 — 지평선 밖에서 맥동하는 두 번째 고리. 물리: 최대 회전 커 블랙홀에서 시공간 자체가 끌려 도는 영역이 지평선 밖까지 뻗는다.
  - 회귀 2건(`verify-escalation` 16→18): 렌징이 실제로 치솟는가 · 고리가 발동 중에만 보이는가.
- **무료함 — 진단**: 이번 세션에 넣은 것(런 리듬 사건·후반 천체·진화 선택·종착점·콤보 7단계)은 **전부 볼거리였지 할 거리가 아니었다.** 블랙홀이 된 뒤 **동사(verb)가 하나뿐**이다 — 능력 4개도 전부 '더 잘 먹기' 또는 '안 죽기'의 변주다. 그래서 안 풀렸다. 지적이 정확하다.
- **시안 `mock/midgame.html`** — 네 선택지를 물리 근거·비용·밸런스 위험과 함께 제시:
  - **A 블랙홀 사냥**(현상금·추적·병합) — 후반의 주 활동을 바꾼다. 네메시스·병합 재사용이라 위험 낮음. 물리: SMBH의 주 성장 경로는 병합이다.
  - **B 펜로즈 과정**(스쳐서 회전 에너지 빼앗기) — 새 동사. "삼킬까 스칠까". 방금 넣은 에르고구면을 그대로 쓴다. 이론상 커 블랙홀 질량의 29%까지 추출 가능.
  - **C 원반 짓기**(삼키기 vs 비축) — 가장 크고 가장 위험. 성장 경제를 다시 짜야 한다.
  - **D 은하 지도**(목적지 다변화) — 또 볼거리라 지금 문제를 직접 풀지 못한다.
  - **추천: A + B.** A가 '무엇을 하는가'를, B가 '어떻게 하는가'를 바꾼다.
- 전 회귀 **448/448**(29스위트).

## 2026-08-02 · 화면을 덮던 펄서 빔을 '등대'로(5.11)
- 오너: **"좌측의 이 원뿔은 뭔가요? 의도가 뭔가요?"** → **펄서의 등대 빔**이다. 실제 펄서는 자기극에서 빔을 쏘고, 그 빔이 지구를 스칠 때마다 깜빡여 보여 '펄스(pulse)'라 불린다.
- **그런데 수치가 잘못됐다**: 빔 길이 6.5가 몸 스케일(`r*0.72`)에 그대로 곱해져, 질량 2.9K에서 **약 62 월드 유닛** — 화면 반높이(~65)를 통째로 덮었다. 게다가 자전(`rotation.y`)은 세로축이라 스윕이 실루엣에 드러나지 않아 **정적인 쐐기**로 읽혔다.
  - 오너가 원뿔 형태를 물은 게 이번이 **세 번째**다(혜성 꼬리 2회 → 펄서 빔 1회). 이 게임에서 원뿔은 그 자체로 안 읽힌다는 신호로 본다.
- **고침**: 원뿔을 6.5×0.45 → **3.2×0.30**으로 줄이고(실측 62 → **41.7유닛**), 자전 위상에 맞춘 **날카로운 깜빡임**을 넣었다 — 평소 0.10, 스칠 때만 0.51(실측). 길이가 아니라 **깜빡임이 펄서를 펄서로 만든다.**
- **하네스 결함 수리**: `setSpawn(false)`가 **지형에는 안 걸려** 있었다. 성운은 초당 +3.5%를 '먹이므로', 호킹 축소 테스트가 간헐적으로 뒤집혔다(6→6.49, 4회 중 1회). `topUpFeats`에 `NO_SPAWN` 게이트를 걸어 5회 연속 통과. — 검증 플래그의 의미는 '먹이 유입 차단'이고 지형도 유입이다.
- `verify-menu`의 'HUD visible' 1회 실패는 격리 3회 통과로 흔들림 확인.
- 전 회귀 **446/446**(29스위트).

## 2026-08-02 · 링 색을 가르치고, 그리드 점을 빼다(5.10) + '안 커진다'의 실측
- 오너: **"초록색 천체 주변의 초록 원은 왜 있나요?"** → 조준 링이다(초록=먹을 수 있음 · 빨강=위협 · 주황=충돌 위험).
  - **오너가 물었다는 게 곧 증거다.** 확인해 보니 안내는 이미 있었다(`◆ EAT THE GREEN` 등). 문제는 **생애 첫 판에만**(`acc_onboard`) 뜨고, 게다가 **시계로**(3.2초·6.4초) 떴다는 것 — 그 순간 화면에 그 링이 없으면 가르침이 허공에 뜬다.
  - **고침 ①**: 시작 카드(`showOnboard`는 매판 호출)에 **HALOS 범례 3줄** 상설. 낮은 화면(≤430px)은 조여 잘림 없음(모바일 가로 50~344/393 실측).
  - **고침 ②**: 링 힌트를 시계에서 **조건**으로 — 그 링이 실제로 가까이 있을 때 뜬다. `verify-depth`의 옛 테스트는 매 반복 `clearObjs()`를 불러 조건이 영영 성립하지 않았으므로 새 계약에 맞게 다시 썼다.
- 오너: **"그리드 중앙의 사각형은 왜 필요한가? 그리드는 필요한가?"**
  - 사각형은 교차점 표식이었다. 세는 데 도우라고 넣었지만 오너에겐 '왜 있는지 모를 점'으로 읽혔다 → **제거**. 자 구실은 선만으로 충분하다.
  - 그리드 자체는 **유지**. 이유는 아래 실측과 직결된다.
- 오너: **"블랙홀이 된 뒤 행성을 먹어도 화면의 다른 블랙홀과 비교해 커지지 않는 것 같다"** → **맞다. 실측:**

  | 질량 | 라이벌 대비 | 화면 점유비 |
  |---|---|---|
  | 25 | ×0.95 | 0.060 |
  | 600 | ×0.88 | 0.096 |
  | 40,000 | ×0.87 | 0.120 |
  | 300,000 | ×0.90 | 0.125 |

  질량 **12,000배**에 라이벌 대비는 **항상 ×0.9**, 화면 점유는 2.1배 늘고 40,000 이후 사실상 정지.
  **뿌리**: `topUp()`의 모든 스폰이 `S.mass × 비율`이다 — 우주가 나에 맞춰 통째로 리스케일되니 아무것도 작아지지 않는다. 그리드(우주 자)는 바로 이걸 보완하려고 넣은 장치라 **지금도 유일한 절대 기준**이다.
- **시도했다가 걷어낸 것**: 라이벌 원반을 질량에 따라 색칠해(T ∝ M^-1/4) '누가 더 큰지' 읽히게 하려 했다. 값은 정확히 써졌으나(#94d6f6…#ff3b14) **렌더에서 가산 글로우가 흰색까지 포화**돼 화면에서 구분되지 않았다 — 스크린샷으로 확인하고 **미검증 상태로 내보내지 않기로 하고 되돌렸다**(진단 코드도 함께 제거).
- 전 회귀 **446/446**(29스위트).

## 2026-08-02 · 상승 곡선을 실제 런 분포에 맞춘다(5.9)
- 5.8에서 넣은 런 열기를 **210초 선형**으로 잡았는데, 실제 데이터에 대보니 잘못이었다.
- **실측(QA 제외·30초 이상 28건)**: 중앙값 **60초** · p75 114초 · p90 217초 · 최장 633초. **210초 이상은 28건 중 4건(14%)**.
  → 중앙값 런은 상승의 **29%만** 겪고, 대부분이 램프를 못 느낀다. 오래 산 소수는 상한에 걸려 평평하다. 최악의 조합이었다.
- **지수 접근으로 교체**: `heat = 1 − e^(−t/70)`. 30초 0.36 · 60초 0.58 · 114초 0.80 · 217초 0.96 — 초반에 빠르게 오르고 끝없이 완만히 오르되 폭주하지 않는다. 나선 낙하의 물리와도 맞는다(중심에는 점근적으로 가까워질 뿐 도달하지 않는다).
- 회귀 2건 추가: 중앙값 런이 램프의 절반 이상을 겪는가 · 긴 런에서도 계속 오르는가(평평한 천장 금지).
- **자체 정정**: 직전 보고에서 "데이터상 절반 이상의 런이 초반에 끝난다"고 했는데, 그 근거는 **내가 이미 오염됐다고 폐기한 QA 트래픽**이었다. 초반 난이도에 대한 근거는 아직 없다 — 예전 `difficulty-early-wall`과 같은 실수를 반복할 뻔했다.
- **초반 구조 감사(난이도 아님)**: PROTOSTAR는 천체 4종·지형 0·이벤트 없음·렌징 0.14, MAIN SEQUENCE는 지형 1, 블랙홀부터 천체 7종·지형 2·이벤트 개시. 오프닝이 가장 마른 구간인 건 사실이나, 성장식으로 계산하면 블랙홀까지 약 14끼(≈25초)라 구간 자체가 짧다 — **문제라고 단정할 근거는 없다.**
- 전 회귀 **446/446**(29스위트).

## 2026-08-02 · 콤보의 96%가 무의미했다 — 상승 3축 신설(5.8)
- 오너: **"콤보 700까지 갈 때까지 큰 변화가 없어 무료하다. 난이도도 상승해야 할 것 같다. 냉철하게 판단해달라."** + 이어서 **"쿠키런은 예시일 뿐, 우리만의 스타일이 있어야 한다."**
- **냉철한 진단 — 재미 요소는 충분하지 않았다. 수치로 확정:**

  | 콤보 | 점수 배수 | 흔들림 | 표시 크기 |
  |---|---|---|---|
  | ×25 | ×3.88 | 0.20 | 1.288 |
  | ×700 | **×3.88** | **0.20** | **1.288** |

  `1+min(c-1,24)*0.12` · `min(c,20)*0.01` · `min(c,24)*0.012` — **셋 다 25에서 상한.** 최고 단계도 ×20이 끝.
  오너가 700까지 갔다면 **675 동안 게임이 아무 반응도 안 했다.**
- **두 번째 구멍**: `S.t`가 난이도 파라미터에 **한 번도** 쓰이지 않았다(grep 확증). 천체 수는 질량에만 비례(상한 있음), 위협 비율 상한 18%, 천체 속도 고정. 성장은 에딩턴으로 로그로 느려지는데 위협은 비례로만 따라오니, 후반은 '더 어려운 도전'이 아니라 **같은 난이도의 반복**이었다. 숙련이 보상받지 않는다 = 리더보드 경쟁의 축이 시간뿐.
- **고친 것 — 상승 3축**:
  1. **콤보 7단계**(5·10·20·40·80·160·320) — HYPERACCRETION · RELATIVISTIC BEAM · MAGNETOSPHERE · EXTREMAL KERR 신설. 점수 배수는 25 이후 `0.35·log2(c/25)`로 계속 오른다(×700 → ×5.56). **로그인 이유**: 선형으로 이었다가 폭주해 리더보드 상한을 깬 전례가 있다. 흔들림·표시 크기 상한도 로그로 완화.
  2. **완전체 EXTREMAL KERR** (오너 아이디어) — 콤보 ×320에서 **10초 래치**. 최대 회전 커 블랙홀(a = M)은 이론적 한계까지 회전해 더는 스핀업이 불가능하고 효율 42%에 이르는, 문자 그대로 '완전한' 블랙홀이다. 발동 중 원반이 백열하고 광자 고리가 두꺼워지며, 끝나면 스핀다운. 콤보 단계는 먹기를 멈추면 바로 풀리므로 최상단만 래치로 붙잡아 절정을 느끼게 했다.
  3. **런 열기(run heat)** — **쿠키런의 속도 슬라이더를 베끼지 않았다.** 이 런은 은하 중심으로의 나선 낙하이고, 중심에 가까울수록 별의 밀도와 궤도 속도가 가파르게 오른다(Sgr A* 근처 S성이 광속의 3%로 도는 이유). 시간 = 중심에 얼마나 다가갔는가. 210초에 걸쳐 밀도 ×1.30 · 라이벌 확률 0.181→0.245 · 천체 속도 ×1.85.
- **신규 스위트 `verify-escalation`(14건)** — "후반이 무료하다"의 근본 원인을 전담으로 막는다. 콤보 곡선이 25에서 멈추지 않는가 · 7단계 존재 · 곡선이 로그라 폭주하지 않는가 · 완전체가 10초 래치 후 반드시 끝나는가 · 열기가 밀도/위협/속도를 실제로 올리는가.
- **하네스 교훈 2건**: ① 위협 '비율 표본'은 노이즈가 커 흔들렸다(18→24.5 / 17→25 / 21→23) → 실제 스폰 파라미터(`spawnMix`)를 노출해 판정. ② 스텝 사이 인덱스로 짝지어 변위를 재면 객체가 바뀌어 무의미하다 → `objInfo`에 속도(v)를 노출해 직접 측정.
- 코덱스 37→38(EXTREMAL KERR). 전 회귀 **444/444**(29스위트).

## 2026-08-02 · 승격이 '나'도 바꾼다 — 원반의 진화(5.7)
- 오너: **"검은색 블랙홀인 상태에서 다음 단계로 진화하나요? 예전에는 완전한 블랙홀의 형태로 변했던 것 같은데요."**
- **확인 결과 — 회귀가 아니라 원래 없던 것.** 이력을 뒤지니 `c36432c`(블랙홀 5단계 진화)에서도 티어가 바꾼 건 제트·존 팔레트·렌징이었고 **블랙홀 메시 자체는 한 번도 티어를 따라 바뀐 적이 없다.**
  - 형태 전환은 딱 두 번: 질량 3(미행성→항성), **질량 20(초신성→블랙홀)**. 오너가 기억하는 "완전한 블랙홀"은 후자다.
  - 그 뒤 8자릿수·5티어 동안 아바타는 **크기만** 커졌다. 승격이 '세상'만 바꾸고 '나'는 안 바꿨다.
- **고친 것 — 물리 근거 둘로 원반을 진화시킨다**:
  1. **색온도** — 얇은 원반 이론에서 같은 에딩턴 비면 `T ∝ M^(-1/4)`. 항성질량의 원반은 X선으로 타는 청백색(~10⁷ K), 초대질량은 훨씬 차가운 원반(~10⁵ K). 티어마다 한 단계씩 식는다(#caf0ff → #dee193).
  2. **원반 반경** — 원반 바깥은 자기중력으로 잘린다. 항성질량은 지평선의 ~10⁵배까지 뻗지만 초대질량 AGN은 ~10³~10⁴배에서 끊긴다 → 고질량일수록 **원반이 지평선에 바짝 붙는다**(바깥 반경 2.0 → 1.56).
- **자체 적발 3건**:
  - **TDZ**: `applyForm`에 `repaintDisk()`를 넣었더니 모듈 초기화 시점의 `applyFormInit()`이 아직 선언 전인 `SKINS`를 건드려 페이지가 통째로 죽었다(`__acc` undefined). 그 호출은 불필요해 제거.
  - **이름 충돌**: `cool`이 이미 방향광 변수였다 → `coolHex`로.
  - **한 티어 지연**: 승격 블록이 `S.tierIdx` 갱신 **전**이라 옛 티어로 칠해졌다 → 새 티어를 인자로 넘긴다.
- **설계 정정**: 처음엔 고정색으로 보간했는데 **AURORA가 올리브색이 됐다**(스킨 정체성 붕괴). 채널 이득(파란 성분만 크게 낮춤)으로 바꿔 어느 스킨이든 계열을 지킨 채 식게 했다. 회귀로 3스킨이 최대 질량에서도 구별되는지 고정.
- **색만으론 부족했다**: 고질량에서 원반이 너무 밝아 색이 흰색으로 씻겨 나가는 걸 스크린샷으로 확인 → 밝기와 무관한 '형태' 신호(원반 반경)를 추가한 이유.
- 회귀 **+4건**(lategame 21→25). 전 회귀 **430/430**(28스위트).

## 2026-08-02 · 런 리듬 이벤트 + 후반 천체 2종(5.6) — 후반 후킹 ①
- 5.5 감사에서 남긴 두 구멍을 메웠다: **후반 천체 구성이 40,000 이후 동일**, **시간이 평평하다(같은 밀도로 계속 먹기만 한다)**.
- **런 리듬 이벤트 2종** — 블랙홀이 된 뒤부터 26~40초 간격으로 교대 발화(원시별 구간은 기본기에 집중하도록 제외):
  - **STELLAR STREAM** — 조석에 찢긴 성단은 흩어지지 않고 궤도를 따라 '줄'로 늘어선다(궁수자리 흐름). 별 13개가 화면을 가로질러 흐르는 수확 창.
  - **SHOCK BREAKOUT** — 이웃 별이 터지면 충격파가 지나간다. 링이 퍼지며 천체와 나를 함께 밀어내고, 지나간 자리에 잔해(질량 보상)를 남긴다.
- **후반 천체 2종** — 퀘이사와 극대질량이 거의 같은 표를 쓰고 있었다:
  - **S-STAR**(퀘이사부터) — 은하 중심을 도는 별. S2는 근점에서 광속의 3%로 스친다 → 게임에서도 속도 ×5.5로 가장 빠른 먹이.
  - **GALACTIC NUCLEUS**(극대질량) — 왜소은하가 삼켜지면 그 핵성단이 중심으로 가라앉아 자기 블랙홀을 배달한다 → 굼뜨고(×0.35) 무거우며 안에 검은 점을 품고 있다.
- 코덱스 4종 추가(33→37): S-STAR · GALACTIC NUCLEUS · STELLAR STREAM · SHOCK BREAKOUT.
- **자체 적발 — 사건이 화면 밖에서 벌어졌다**: 흐름 배치에 `22+hr*7`을 시야로 썼는데 그건 **스폰 기준**이지 시야가 아니다. 실측하니 camDist 56일 때 그 값은 78이라 사슬 13개가 전부 화면 밖이었다 — 배너만 뜨고 아무 일도 안 보이는 상태. 카메라 거리 기준(`off=R*0.5~0.8`, `step=R*0.40`)으로 바꿔 사슬이 화면을 가로지르게 했다.
- **하네스 보강**: `objInfo()`에 좌표(x·z·d)와 `eaten`을 넣었다 — 좌표가 없어 거리 계산이 NaN이 되며 **두 번 헛돌았던** 사각지대다. `viewR()`(V·camDist)도 추가.
- 회귀 **+7건**(lategame 14→21). 코덱스 총수를 33으로 하드코딩한 8개 스위트도 갱신. 전 회귀 **426/426**(28스위트).

## 2026-08-02 · 아무도 닿은 적 없던 종착점을 열었다 — 초·중·후반 전수 감사(5.5)
- 오너: "초반/중반/후반 전방위적으로 재미 요소가 잘 설계돼 있는지 잘 판단해서 진행". 감이 아니라 **계측**으로 지형을 그렸다.
- **감사 결과 — 초반·중반은 촘촘하다**: 질량대별 천체 구성을 실측하니 rock/comet → planet·star·whitedwarf·neutron·redgiant·giant → binary·hypergiant·pulsar → cluster·gascloud로 계속 갈아탄다. 형태 전환(초신성)·천적 등장·슬롯 1→2·존 색·진화 선택 2회·제트 해금이 순차로 들어온다.
- **후반 3중 정체 적발**:
  1. **천체 구성이 멈춘다** — 40,000 / 120,000 / 1e6 / 1e9의 구성이 사실상 동일.
  2. **맵이 멈춘다** — `topUpFeats`가 블랙홀이 된 뒤로 지형 **2개 고정**, 종류·확률도 동일.
  3. **눈금이 멈춘다** — `accEff ∝ 1/m`이 먹이의 비례 크기를 상쇄해 **한 끼 이득이 절대값 ~108로 수렴**한다(병합은 ~1,080). 성장이 선형인데 티어는 ×10씩 요구한다.
- **결정적 사실 — ②는 이미 있었다**: `CORE_MASS=4.3e6`(실제 Sgr A\* 질량)의 은하 중심이 이미 구현·배치돼 있었다. 내 계획("②를 만든다")이 틀렸다. 진짜 문제는 **조건**이었다 — 삼키려면 질량 2,365,000이 필요했는데, **라이브 DB 실측 544런 중 도달 0건**, 역대 최고 런도 209,304(8.8%). 게임이 "종착점"이라 부르는 곳에 **아무도 닿은 적이 없었다**.
- **고친 것**:
  - 종착점 조건: 질량 2,365,000 → **퀘이사 티어(제트가 점화되는 등급)**. 그 아래로 접촉하면 여전히 잡아먹힌다. 보상은 일회성 **+1,935,000**(합체는 밀어낼 가스가 없어 에딩턴 감쇠를 받지 않는다).
  - **나침반**: 초대질량부터 우측 HUD에 `CORE ↑ 1.5K` — 방위 8칸 + 거리, 도전 가능해지면 초록.
  - 후반 맵: 초대질량부터 지형 2 → 3.
- **자체 적발 — 폭주**: 처음엔 "병합은 에딩턴 한계를 안 받는다"며 감쇠를 통째로 없앴다(물리적으로는 맞다). 그랬더니 `verify-ecology`의 완전 플레이 220초가 질량 **7.4e17**로 폭주했다. 감쇠는 필요했다 — 되돌리고, 정체의 해법을 경제가 아니라 **조건**에서 찾았다. 회귀로 제동을 고정(+30% @500 → +0.14% @500K).
- **엣지 함수 v7**: 코어 보상이 일회성 +1.935M이라 성장 가드(`mass > 20000*(dur+3)`)가 **90초쯤 삼킨 정당한 런을 거부**했다. 그 몫만큼 상수 허용치(CORE_BONUS 2M)를 열었다. 성장 '속도' 제한은 그대로.
- **하네스 결함 4건**(전부 제품 결함으로 오인할 뻔): ① `step()`이 `update()`만 돌고 **`updateCore()`를 안 불러** 종착점을 볼 수 없었다(실제 `frame()`과 맞춤). ② 조종 입력을 안 줘 먹이가 소비되지 않았다. ③ "드레인 ×0.86"의 정체는 호킹 복사가 아니라 **라이벌 조석**이었다(실제 드레인은 절대값 0.05/s라 후반엔 무시할 수준). ④ `objInfo()`에 좌표가 없어 거리 계산이 NaN.
- **테스트 데이터 누적**: `verify-daily` 2건이 3회 연속 실패 — QA 데이(2000-01-01)에 **699행**이 쌓여 제출이 140위로 밀려 top-100 조회 밖이었다. 실제 데일리 보드 격리는 유지되고 있었다(오염 0). 행 정리 + 제출 점수를 허용 상한 근처로, 조회 범위 100→500으로 견고화.
- 회귀 **+8건**(lategame 7→14, 신규: 코어 도달·삼킴·미달 시 사망·나침반 2·폭주 제동·지형 증가). 전 회귀 **419/419**(28스위트).

## 2026-08-01 · 다국어(EN/KO) — 포털 제출 대비(5.4)
- 오너: **"영문화를 하셔요. 오로지 영문으로만 가능하다는 말씀이시죠?"** → **아니다.** 포털(CrazyGames·Poki) 요건은 "영어로 플레이 가능할 것"이지 "영어만 있을 것"이 아니다. 둘 다 다국어를 권장한다.
- **한글 노출 실측 = 진화 카드 12개 문자열뿐**(요약 6 + 물리 근거 6). 나머지 한글은 소스 주석과 개발 로그라 화면에 안 나온다. 범위가 작아 영문 전용이 아니라 **양쪽 다** 가는 게 싸게 먹혔다.
- **언어 결정 순서**: `?lang=` → localStorage → `navigator.language` → en. 한국어 브라우저는 한국어를, 포털 리뷰어는 영어를 본다. 시작 화면 `🌐 ENGLISH / 🌐 한국어` 토글(CODEX·LEADERBOARD·AWARDS와 같은 줄).
- `T(o)` 하나로 렌더 지점 3곳(카드 요약·근거·획득 배너)에서 꺼내 쓴다. 문자열이면 그대로, `{en,ko}`면 현재 언어.
- **신규 스위트 `verify-i18n`(11건)** — 제출을 지키는 자동 가드:
  - 영어 모드에서 **렌더된 텍스트 437개를 훑어 한글 0자**(선택 카드·코덱스·배너·결과 화면까지 열어서 검사. `<script>`는 화면이 아니므로 제외 — 주석은 한글로 남는다).
  - 한국어 브라우저는 여전히 한국어(기능을 잃지 않았다는 반대 방향 검사).
  - `?lang=en`이 한국어 브라우저를 덮어쓴다 · 토글이 라벨·저장까지 바꾼다 · 토글이 시작 화면에서 실제로 보인다.
  - **데이터 차원**: 6종 × (요약+근거) × 2언어가 모두 있고, 영어 문자열에 한글이 섞이지 않았다.
- 전 회귀 **412/412**(28스위트).

## 2026-08-01 · 콤보를 상단 중앙으로 + 진화 카드 직관화(5.3)
- 오너: **"콤보가 밑에 나와서 잘 안 보인다 — 중앙 상단은?"**, **"아이템별 특장점이 직관적으로 이해되지 않는다"**.
- **콤보 → 상단 중앙**. 상단은 이미 질량바·APEX·배너가 쓰고 있어 자리를 새로 계산했다: 질량바 14~44 · **콤보 50~128** · APEX 136~160 · 배너 166~. 낮은 화면(≤430px)은 46/104/134로 조인다. 콤보 확대 배율도 ×1.48→×1.29로 낮췄다(그 배율이 박스를 89px까지 부풀려 APEX와 겹쳤다).
- **카드 재설계 — 글자 나열 → 읽히는 구조**:
  1. **도식** 6종(SVG) — 두꺼운 원반·쌍극 제트·붙잡는 자기력선처럼 그 진화가 블랙홀에 무슨 일을 하는지 글자보다 먼저 보인다.
  2. **플레이 언어 한 줄** — "한 입이 빨라진다 — 대신 닿는 거리가 준다". `accretion +18%` 같은 지표명은 게임 안에서 배운 적 없는 말이었다.
  3. **발산 막대** — 가운데가 0, 이득은 오른쪽 초록, 손해는 왼쪽 주황. 세 카드의 막대 높이를 맞춰(요약 줄 수가 달라도) 나란히 비교된다.
  4. 물리 근거는 구분선 아래로 내려 작고 흐리게 — 근거는 남기되 결정을 방해하지 않는다.
- **순수 이득 카드 2종을 맞교환으로**(물리가 먼저 답을 줬다): COLD FLOW는 차가운 흐름이 넓은 헤일로가 아니라 **가는 필라멘트**로 오므로 reach −6%; MAGNETIC ARREST는 그 이름대로 자기장이 유입을 **"붙잡아(arrest)"** 강착률에 뚜껑을 씌우므로 swallow −8%. 이득만 있는 카드가 있으면 그것만 고르게 되어 선택이 사라진다 — 회귀로 6종 전부 맞교환임을 고정했다.
- **자체 적발 결함 1건**: 콤보 **연쇄 판정**(`S.combo+1` 조건)이 `comboWin()`이 아니라 1400ms 하드코딩이라, **MAD의 +60%가 실제 연쇄에는 적용된 적이 없었다**. 카드가 약속한 것을 게임이 안 하고 있었다. 표시 판정도 같은 하드코딩이라 함께 교체.
- **하네스 결함 2건**: ① SwiftShader는 컴포지터가 안 돌아 **CSS 트랜지션이 중간값(0.176)에 얼어붙는다** — 컴퓨티드 opacity로 가시성을 판정하면 안 된다(hud가 내린 인라인 결정을 읽도록 교정). ② 1프레임이 **100~280ms**로 요동쳐 벽시계 대기도 고정 나이도 못 쓴다 — 프레임 비용을 먼저 재고 판별 구간 한가운데로 나이를 역산한다.
- **판단 정정**: 그 사이 "MAD 표시가 안 뜬다"고 적었으나 **제품 버그가 아니었다** — 200ms 대기 동안 hud()가 한 번도 안 돈 것뿐이다.
- `verify-ui`에 APEX 강제 표시 추가(상단 밴드 사각지대였다). 6뷰포트×2모드 **겹침 0**.
- 회귀 **+6건**(evolution 28→34). 전 회귀 **401/401**(27스위트).

## 2026-08-01 · 티어 승격마다 진화 선택 3택(5.2) — 후반 후킹 ③
- 오너: **"몇 번 플레이하면 금세 무료해질 것 같다 · 후반 후킹이 약하다"** → 승인된 순서 ③→②→① 중 **③ 착수**.
- **왜 선택인가**: 지금까지 성장은 자동이었다 — 질량이 오르면 정해진 티어가 온다. 매 런이 같은 곡선이면 두 번째 플레이에 새로울 게 없다. **분기가 있어야 다음 런이 다른 런이 된다.**
- **EVO_BOONS 6종 — 전부 실제 강착 물리의 트레이드오프**(하나도 임의 수치가 아니다):
  - `RAPID SPIN` 강착 +18% · 반경 −8% — 최대 회전 커 블랙홀은 ISCO가 안쪽으로 내려가 효율이 **6%→42%**. 대신 지평선은 작다.
  - `THICK DISK` 반경 +15% · 속도 −8% — 강착률이 높으면 복사압으로 원반이 부푼다(slim disk). 넓지만 둔해진다.
  - `RELATIVISTIC JETS` 제트 +35% · 강착 −8% — 회전 에너지를 제트로 빼면 그만큼 삼키지 못한다.
  - `COLD FLOW` 흡수 시간 −22% — 차가운 가스는 데워질 필요 없이 곧장 떨어진다(cold accretion).
  - `MAGNETIC ARREST` 콤보 창 +60% — MAD 상태의 자기장은 유입을 붙잡아 오래 지속시킨다.
  - `WIDE HORIZON` 반경 +14% · 강착 −5% — 저회전은 지평선이 크지만 효율이 낮다.
- **적용은 선언이 아니라 계산에**: `boonMul(k)`를 실제 8곳에 곱한다 — 강착 효율 2곳(단일·병합) · 흡입 반경 · 최대 속도 · 흡수 지속 · 콤보 창 · 제트 반동 · 제트 사거리. 회귀는 "보온을 골랐다"가 아니라 **"흡입 반경 숫자가 20.63→23.52로 변했다"**를 본다.
- 선택 중엔 `frame()`이 `update()`를 멈춘다 — 읽는 동안 죽지 않는다. 키 1/2/3 · 카드 탭 둘 다.
- **자체 적발 결함 1건**: 선택 화면이 떠 있는데 **Space/Shift/E/F 능력 키가 그대로 발동**했다. 멈춘 세계에 대고 에너지와 쿨다운만 잃는 상태 — 카드 읽다 스페이스 누르면 충전한 대시가 증발한다. 키 핸들러 + 능력 함수 4개 양쪽에 잠금(경로가 늘어도 안 뚫리게).
- **검증 하네스 결함 1건**: 첫 회귀가 `A.state.tierIdx`(존재하지 않는 필드)를 읽어 재승격이 한 번도 안 일어났고, "중복 없음"이 **공짜로 통과**하고 있었다. 별도 프로브로 4티어 전부 실측해서야 드러났다 — 통과한 테스트도 실제로 무엇을 실행했는지 본다.
- `verify-ui`: 선택 오버레이도 회전 안내와 같은 **전체 화면 모달**로 처리(뒤 요소는 보이지 않는다). 겹침 61 → 0.
- **스킵 플래그 재설계(자체 적발 2건째)**: 처음엔 `?dev=1`이면 선택창을 통째로 껐다 — 그러면 **dev 링크로 여는 사람에겐 기능이 사라진다**. `setMass`로 티어를 **위조한 런**에서만 건너뛰도록 좁히고(`begin()`에서 해제), '먹어서 오른 티어에서도 뜬다'를 별도 회귀로 고정했다(질량 121·INTERMEDIATE 실측).
- 신규 훅 `tierMass`·`giveBoon`·`pullRadius`·`choiceReal`. 회귀 **+10건**(evolution 18→28).
- 전 회귀 **395/395**(27스위트). *직전 로그의 '392'는 합산 오기 — 같은 방식으로 세면 5.1 시점은 385였다.*
- **미해결 1건**: 전체 스윕 중 `verify-daily`가 한 번 15/16(항목 미기록). 격리 16회 연속 통과로 재현 실패 — 원인 미상. 다음 스윕부터 실패 줄을 남기도록 게이트 명령을 바꿨다.

## 2026-08-01 · 콤보를 보이게 + 단계별 효과(5.1)
- 오너: **"콤보 표기가 우측 상단이라 잘 안 보인다"** + **"콤보 숫자별 효과도 다르면 재밌겠다"**.
- **위치**: 우측 상단(점수·시간 옆) → **화면 중앙 하단**, 능력바 바로 위. 크기도 26→44px로 키우고 단계명을 아래 줄에 붙였다. 콤보는 '지금 잘하고 있다'는 즉시 피드백이라 시선이 머무는 곳에 있어야 한다.
- **단계별 효과 3종 — 전부 실재하는 현상**:
  - `×5` **SUPER-EDDINGTON** — 강착 효율 ×1.14. 고전 에딩턴 한계를 넘겨 물질이 쏟아져 들어가는 상태(밝은 ULX가 증거).
  - `×10` **DISK FLARE** — 효율 ×1.28 · 흡입 반경 ×1.18. 강착률이 치솟으면 원반이 부풀고 영향권이 넓어진다.
  - `×20` **BLAZAR STATE** — 효율 ×1.50 · 반경 ×1.30 · **동시 흐름 슬롯 +1**. 강착 급증이 제트를 밝히고 여러 흐름이 동시에 합류한다.
- 단계 진입 순간만 배너로 알린다(`S.boonAt`로 중복 방지), 콤보가 끊기면 초기화.
- 회귀 3건 신설(`verify-features`): 5·10 도달 · 20에서 슬롯 4로 증가.
- **패치 사고 1건 자체 적발**: COMBO_BOONS 배선 스크립트가 중간 단언에서 멈춰 **전부 미적용**인데 `hud()`는 이미 `comboBoon()`을 부르고 있었다 — 콤보가 뜨는 순간 터질 상태였다. 스모크에서 콤보가 안 떠 오류가 안 났고, 함수 존재를 grep으로 확인해서야 잡았다. 교훈: **여러 앵커를 한 스크립트에 묶으면 부분 적용이 남는다** — 치환 후 심볼 존재를 반드시 확인한다.
- 전 회귀 **392/392**.


## 2026-08-01 · 혜성이 '탐조등'으로 읽히던 문제(5.0.1)
- 오너: **"작은 행성에서 분출되는 플래시 같은 건 뭔가요?"** — 혜성 꼬리다. 방향은 4.1.1에서 이미 고쳤는데, **두 번이나 "저게 뭐냐"고 물었다는 건 혜성으로 안 읽힌다는 뜻**이다.
- 원인: 핵이 맨 구체이고 꼬리가 **경계가 뚜렷한 원뿔 하나**라 방출기(탐조등)처럼 보였다. 옆면이 선명한 게 결정적이었다.
- 혜성의 인상을 만드는 두 가지를 넣었다:
  1. **코마** — 승화한 가스가 핵을 감싼 흐릿한 구름. 혜성을 혜성으로 알아보게 하는 가장 확실한 표식.
  2. **두 갈래 꼬리** — 이온(좁고 곧은 청백)과 먼지(넓고 흐리며 0.3rad 뒤처짐). 실제 혜성의 특징이고, 두 개가 겹치면 단일 원뿔의 인상이 깨진다.
- 꼬리 색을 **길이 방향뿐 아니라 옆으로도** 감쇠시켰다(`side` 항) — 옆면이 선명하면 무엇을 해도 원뿔로 보인다.
- 방향 회귀는 이온 꼬리로 잰다(먼지 꼬리는 의도적으로 뒤처지므로). `verify-lore` 통과.
- 전 회귀 **389/389**.


## 2026-08-01 · 후반 병목 해소 A+B(5.0)
오너 승인으로 회고안 A(동시 조석 흐름)와 B(흡수를 능동적으로)를 구현했다.

**A — 흡수 병렬화**
- `S.feed`(단일) → `S.feeds`(배열). 슬롯 수는 티어가 정한다: 항성질량 1 · 초대질량 2 · 퀘이사 이상 3.
- 시각 자원도 슬롯별로 나눴다(코어 관 + 잔해 입자 세트 ×3). 참조 22곳 전환.
- 물리 근거: 초대질량 블랙홀은 한 번에 하나만 찢지 않는다. 실제 강착원반은 여러 갈래 흐름이 동시에 합류해 만들어진다 — **오히려 예전이 덜 정확했다.**
- 실측: **슬롯이 다 차 더는 못 먹는 시간 77~79% → 4~17%.** 20초 성장은 항성질량 127배 · 중간 13.4 · 초대질량 2.4 · 퀘이사 1.13 · 극대질량 1.01로, 강착 효율이 질량에 반비례하는 기존 설계 덕에 후반 폭주는 없었다(별도 하향 불필요).
- 게이지 라벨에 `DEVOURING ×N` — 슬롯이 늘어난 게 보여야 성장이 체감된다.

**B — 흡수에 개입**
- SPIN(프레임 드래깅)이 **진행 중인 모든 흐름을 38% 앞당긴다**(실측 t 0.04 → 0.48). 실제로 프레임 드래깅은 원반 안쪽 물질을 끌어당긴다.
- 이미 있던 "파고들면 빨라진다"(1/r³)를 화면에 드러냈다 — 게이지가 호박색으로 바뀌고 `CLOSER = FASTER`.

**UI 자리 구조 재정리(파생 작업)**
- 게이지를 감사 대상에 넣자(FORCE_UI) 하단이 이미 붐빈다는 게 드러났다 — 좁은 화면에선 질량바가 아래로 내려오고 코덱스·음소거가 같은 구역이다. 4.9에서 게이지를 내린 자리가 코덱스와 겹쳤다(codex⨯feedlbl 172×10).
- **자리로 분리했다**: 상단 = 메시지 띠(배너·코덱스·APEX), 하단 = 게임 HUD(질량바·게이지·능력·음소거). 배너와 코덱스는 우선순위 큐가 동시 표시를 막으므로(회귀로 고정) **같은 자리를 안전하게 공유**한다.
- 감사도 현실에 맞췄다: `forceUI('b'|'c')` 두 모드로 나눠 잰다(둘을 동시에 띄우면 현실에 없는 조건). 회전 안내 모달이 떠 있으면 그 뒤는 재지 않는다. 획득 팝업(.pop)은 천체 좌표에 뜨는 0.6초짜리라 제외 — 감사가 무작위로 흔들리던 원인.
- 6개 뷰포트 × 2모드 **전부 겹침 0**, 4회 연속 안정.

전 회귀 **389/389**.


## 2026-08-01 · 후반부 회고 — 두 지적이 한 뿌리였다
- 오너: **"후반부로 갈수록 특별한 게 없어 무료하다"** + **"흡수 중엔 다른 행성을 못 먹는 것 같다"**. 실측해 보니 **같은 원인**이었다.
- **흡수에 묶인 시간 비율**(봇 20초 × 각 구간): 항성질량 39% · 중간질량 46% · 초대질량 36% → **퀘이사 79% · 극대질량 77%**. 1회 평균 흡수도 1.29초 → 1.98초.
- **흡수 중엔 큰 천체를 전혀 못 먹는다**(실측): 0.1×·0.3×·0.55×는 먹히는데 0.8×·1.4×는 흡수분 외 증가 **0**. 코드가 동시에 하나만 흡수한다.
- 먹이의 **42~45%가 '내 질량의 60% 이상'**(전 구간 동일). 후반은 화면 천체 수도 56 → 30으로 줄어 사이에 먹을 작은 것도 적다.
- 결론: 후반 플레이 = **2초 애니메이션 → 잠깐 이동 → 다시 2초**. 성장 수치는 오르는데 플레이어가 하는 일이 늘지 않는다. 이게 "특별한 게 없다"의 정체다.
- 개선안 6종을 `mock/retro.html`로 제시(각각 물리 근거·효과·위험·비용 명시). 권장은 **A(동시 조석 흐름) + B(흡수를 능동적으로)** 를 먼저, 그다음 **C(은하 중심을 후반 목적지로)**. F(흡수 시간 상한)는 단독으로는 권하지 않는다 — 오너가 직접 요청한 "큰 천체는 시간이 걸린다"를 약화시키므로 A·B 뒤 미세조정으로만.
- 오너 선택 대기.


## 2026-07-31 · DEVOURING 게이지 겹침 + 줄기가 홀로 스며들게(4.9)
**① 게이지와 안내 배너 겹침** — 오너 리포트. 여러 기기 크기로 재니 **568×320(아이폰 SE 가로)에서 실제 겹침 116×19**, 640×360은 간격 20px로 아슬아슬했다. 낮은 화면에서 배너가 두 줄이 되며 아래로 내려와 부딪힌다.
- `@media (max-height:430px){#feedwrap{bottom:64px}}` — 음소거 버튼 위로 내려 간격을 벌렸다. 10개 기기 크기에서 간격 **52~495px**, 하단 요소(음소거·질량바·능력버튼·크레딧) 충돌 0.
- **감사가 이 게이지를 한 번도 못 재고 있었다.** `forceUI()`가 `.on`을 붙여도 `hud()`가 매 프레임 지웠다. `FORCE_UI` 플래그를 만들어 hud가 존중하게 했다 — 예전 "감사 훅이 필요했다"와 같은 종류의 사각지대.

**② 줄기가 홀 근처에서 끊긴다** — 두 가지를 고쳤다.
- 경로에 **감아 도는 구간**을 덧붙였다. 원반 테두리에서 끝나던 것을 지평선 근처(0.95hr)까지 2.4rad 감아 들어가게 — 실제 강착의 원형화 후 나선 낙하.
- 페이드를 **곡선 매개변수 → 홀까지의 거리**로 바꿨다. 매개변수로 자르면 글로우 언저리에서 뚝 끊겨 보였다. 이제 4.2hr에서 시작해 2.6hr에서 소멸하는 smoothstep이라 **빛 속으로 스며들며** 사라진다. 코어 관에도 같은 규칙을 적용(관만 매개변수 페이드라 선이 잘려 보였다).

- 전 회귀 **383/383**. `verify-ui` 정착 대기 0.7→1.3초(배치에서 한 번 거짓 겹침이 났다).


## 2026-07-31 · 나선 줄기가 자라나 연결되게(4.8)
- 오너: **"흡수가 시작되면 나선고리가 갑자기 뚝 생겨 이질적이다. 해당 행성으로부터 내 블랙홀까지 자연스럽게 연결되게 못 하나."** — 맞다. 줄기가 완성된 길이로 한 프레임에 나타났다.
- **본체 쪽(u=1)에서 홀 쪽(u=0)으로 자라게** 했다. `f.grow`가 0→1로 차오르고 그리는 구간이 `u∈[1-ease, 1]`로 넓어진다. 이징은 `1-(1-g)²` — 처음엔 빠르게 뻗고 끝에서 부드럽게 닿는다. 지속은 `min(0.45s, 흡수시간×0.45)`라 짧은 흡수에서도 비율이 유지된다.
- 밝기와 조각 수도 같이 차오른다(아직 도달하지 않은 구간엔 조각이 없다).
- 물리적으로도 맞다 — 실제 조석 파괴에서도 잔해가 뜯겨 궤도를 돌아 도달하기까지 시간이 걸린다.
- 실측: 길이 **16.5 → 105.3**(0.54초). 회귀 신설 `줄기가 한 번에 나타나지 않고 자란다`(단조 증가 + 2.5배 이상).
- 전 회귀 **383/383**.


## 2026-07-31 · 나선 줄기가 '굵은 선'으로 보이던 문제(4.7)
- 오너: **"나선고리가 굵은 보더처럼 표현되고, 블랙홀 근처에서 끊기는 것 같고, 조각들이 빨려가는 게 아니라 굵은 선으로 보인다."** 실제 그림을 세 질량대(300·8000·74000)에서 찍어 확인했고 전부 사실이었다.
- **원인 세 개**:
  1. **붙잡는 거리가 밝은 코어 안이었다.** `2.16hr`는 강착원반 바깥이지만 광자링·글로우까지 합친 밝은 덩어리는 그보다 크다. 본체가 통째로 지워지고 **글로우 밖으로 삐져나온 토막만** 보여 "굵은 선"이 됐다. → **4.6hr**로 이동. 실제 조석 파괴 반경은 원반보다 수백 배 바깥이라 물리적으로도 이쪽이 맞다.
  2. **입자가 서로 겹쳐 뭉쳤다.** 크기 `rem*0.42`에 220개를 짧은 곡선에 뿌리니 간격보다 지름이 훨씬 커 연속된 띠가 됐다. → 크기 `rem*0.15`, 확산 0.30→0.55.
  3. **안쪽 페이드가 너무 길었다.** 마지막 16% 구간을 지워 글로우에 닿기 전에 사라져 '끊긴' 것처럼 보였다. → 5%로 단축.
- 코어 관 불투명도도 0.42 → 0.16으로 낮췄다. 조각이 주인공이고 관은 길만 암시한다.
- 회귀 신설: **"흡수 중인 본체가 밝은 코어 밖에 선다"**(실측 4.44hr ≥ 기준 3.6hr). 이 값이 깨지면 같은 증상이 재발한다.
- 전 회귀 **382/382**.


## 2026-07-30 · 흡수 중 천체가 밀려나던 문제(4.6)
- 오너: **"모바일에서 행성과 블랙홀이 충돌하면 밀려나는 것 같은데 개연성이 떨어지지 않나요?"** — 맞다. **내가 넣은 로슈 유지 클램프가 원인**이었다.
- 실측(상대 크기별로 직진해 박으며 천체 변위 측정): **점진 흡수 대상만 밀린다** — 큰천체(0.9배) 38 · 거대(1.6배) 71 · 항성단계 9. 작은 천체는 0. 원인은 `nd=Math.max(want, …)` 하드 하한 — 전진하는 나를 피해 천체를 매 프레임 밖으로 재배치했다.
- **물리는 정반대다.** 조석력은 1/r³이라 블랙홀이 다가가면 잔해는 물러나는 게 아니라 **더 빨리 찢긴다.** 그래서:
  - 밖으로는 밀지 않는다(안으로만 당긴다).
  - 파고든 깊이만큼 **진행도 가속**(최대 1.5배로 제한 — 무제한이면 램밍만으로 0.2초에 끝나 "큰 천체는 시간이 걸린다"는 약속이 깨진다).
  - 파고든 깊이만큼 **잔해가 더 빨리 벗겨진다**(시각적 겹침을 밀어내지 않고 해결).
  - 차례를 기다리는 큰 천체도 밀지 않고, 즉시 삼키지도 않고, **더 끌어당기지도 않는다**(아직 강착 전이다).
- 결과: 진짜 반발(거리 증가) **38~71 → 2.6**(지평선 반경의 0.4배, 육안 식별 불가). 가만히 있을 때 원반 겹침 여전히 0. 흡수 시간 1.2초 유지(램밍 시 0.95초).
- 회귀를 새 계약으로 재작성 — 오너 리포트 2건을 **동시에** 지킨다: ① 가만히 있을 때 원반과 안 겹침 ② 다가가도 안 밀림. 예전 해법은 ①을 고치며 ②를 만들었다.
- **한 수정에서 실수 3개를 스스로 잡았다**: ① `break`로 루프 전체를 빠져나감 ② `const` 선언 전 참조(TDZ) 두 번 ③ **줄 끝 주석이 뒤 코드를 삼켜** `const s=…`가 주석 안으로 들어갔다(기록해 둔 금지 규칙을 내가 어겼다 — 치환할 때 주석은 반드시 자기 줄에).
- 전 회귀 **381/381**.


## 2026-07-30 · 리더보드 상한 개정 배포 + 테스트 행 정리(서버)
- 오너 승인으로 Edge Function `accretion-submit` **v6 배포** + DB CHECK 제약 완화 + 테스트 행 삭제까지 완료.
- **상한이 세 겹이었다.** Edge Function 규칙을 고친 뒤에도 1억 3천만 런이 거부돼 찾아보니 **DB CHECK 제약** `score < 1e8`이 따로 있었다. 함수만 보고 "고쳤다"고 했으면 또 헛수고였다 — 실제 제출로 확인해서 잡았다.
- **평평한 속도 바는 형태가 틀렸다.** 점수는 질량이 복리로 커지면서 시간에 대해 가속한다. 그래서 상수 천장은 후반엔 너무 빡빡하고 초반엔 너무 헐겁다. 실측 `score/dur²`가 안정적이라 **지속시간의 제곱**을 천장으로 삼았다.
  - `score > 4000·dur² + 2e5` → 거부. 오너 최고 런은 15배 여유, "10초에 10억점" 치트는 1600배로 걸린다.
  - `mass > 20000·(dur+3)` → 거부(속도만 보면 '짧은 시간에 거대 질량'을 놓친다).
  - `score ≥ 1e10` → 거부(순수 sanity). DB 제약도 1e8 → 1e10.
  - 574건 전수 대입 위반 **1건뿐**이고 그마저 개발 훅 테스트 행(3초에 질량 3만)이다 — 치트와 같은 모양이라 정상 판정.
- 폐기한 대안 2개를 기록으로 남긴다: ① `score/dur > 120k` 고정 바(실측 최고가 이미 92%) ② `score ≤ k·mass²`(실제 570건에 정상 기록 2건 거부 — 비율이 초반 0.13→후반 0.016으로 떨어져 형태가 틀림).
- 실제 제출 검증: 정상 4종(오너 최고 런·240초 1.33억·600초 12억·짧은 15초 런) **전부 수락**, 조작 5종(질량100에 10억점·10초에 질량100만·초당130만·2초에 100만점·100억점) **전부 거부**.
- 실제 날짜 보드의 테스트 행 **66건 삭제**. 현재 글로벌 보드 103건 · **테스트 행 0건**.
- `verify-lb`/`verify-daily`/`verify-meta` 새 서버 기준으로 통과(15·16·23).


## 2026-07-30 · 자체 플레이 감사 — 리더보드가 강한 런을 거부한다(4.5.1)
오너: "플레이만 하면 되나요? 잠재적 이슈 없이 꼼꼼히 확인하셨나요? 직접 플레이해 보시면서요." → 단계별로 전 구간을 플레이하고 전이·레이아웃을 감사했다.

**통과한 것**
- 5개 구간(원시성·항성질량·중간질량·퀘이사·극대질량) 각 60초 플레이 — **JS 에러 0 · 정지 프레임 0**. 만난 천체 종류 최대 **12종**.
- 플레어 실사용 확인(중간질량 10회·퀘이사 21회·극대질량 15회).
- FLARE 버튼 수명: 시작 메뉴 숨김 · 퀘이사 노출 · 사망 화면 숨김 · 재시작 숨김 ✓
- 능력 버튼 4개 레이아웃: 393·360·1440px 전부 겹침 0 · 화면 안 ✓
- `forceUI()`에 FLARE 버튼을 포함시켜 **4버튼 상태도 겹침 감사 대상**으로(숨은 상태만 재던 사각지대).

**적발 — 배포 차단급**
- **리더보드가 강한 런을 거부한다.** 서버 규칙은 둘: `점수 ≥ 1억` 즉시 거부, `초당 12만점` 초과 거부. 무적 상태로 이어서 플레이한 실측:
  - **120초(질량 44k)**: 초당 15.3만 → 속도 상한 초과
  - **240초(질량 108k)**: 총 1.33억 → 점수 상한도 초과
  - 600초(질량 27만): 총 12억 · 초당 200만
  - 오너 실제 런은 질량 74k·278초·1350만점 = 초당 4.8만으로 통과하지만, 여기서 2배만 더 가면 조용히 거부된다.
- 원인은 **점수가 질량에 선형 비례하는데 질량 자체가 지수적으로 커지는 것**(score ≈ mass^1.9). 내가 넣은 플레어 점수식(`mass*4.6`)도 후반에 폭주해 함께 고쳤다 — 이제 '먹었다면 얻었을 것의 35%'로 묶여 있다(기존 설계 복원).
- **속도 기준 자체가 이 점수 곡선에서 무용하다**: 치트는 130만/초였는데 정상 고질량 플레이가 200만/초에 닿는다. 대안은 **질량-점수 정합성 검사** — `score > 0.25·mass² + 2e5` 면 거부. 실측 비율은 0.005~0.016이라 12배 이상 여유가 있고, "질량 100인데 10억점" 같은 치트는 그대로 걸린다.
- **권고안을 데이터가 반박했다.** 처음 제안한 `score > 0.08·mass² + 2e5`(질량-점수 정합성)를 실제 570건에 대입하니 **기존 정상 기록 2건을 거부**했다(1.02×·1.40× 초과). 점수/질량² 비율이 초반 0.13 → 후반 0.016으로 **떨어지기** 때문에 질량²은 애초에 맞는 형태가 아니었다. 모델을 억지로 세우지 않고 **실측 분포에 여유를 둔 상한**으로 바꿨다.
- 확정 권고(570건 전수 대입 위반 0):
  - 속도 `score/dur ≤ 600,000/초` — 실측 최고 **110,499**(기존 12만 바의 **92%**! 다음 좋은 런 하나면 거부됐다) → 5.4배 여유. 관측된 치트 130만/초는 2.2배로 걸린다.
  - 성장 `mass ≤ 20,000·(dur+3)` — 실측 최고 5,000/초 → 4배 여유. 속도만 보면 '짧은 시간에 거대 질량'을 놓치는데 이게 그 구멍을 막는다.
  - 상한 `score < 1e10`(기존 1e8) — 실측 최고 1,417만 → 700배 여유, 순수 sanity.
- **Edge Function 배포와 소스 파일 저장이 모두 권한 정책에 막혔다.** 오너에게 대시보드에서 세 줄만 고치도록 전달(라인 단위 diff 보고).


## 2026-07-30 · 제트는 평상시 보이지 않는다(4.5)
- 오너 확인 질문: **"제트는 기본적으로 보이지 않고, F/모바일 키를 눌렀을 때 순간적으로 분출하는 파처럼 나오고 사라지는 건데 그렇게 구현돼 있나요?"** — 아니었다. 평상시에도 불투명도 0.17로 흐릿하게 계속 보였다. 정정.
- **가용성과 가시성을 분리**했다. `jetsReady`(퀘이사 티어면 true — 능력 사용 가능·버튼 노출)와 `jets.visible`(발사 순간만 true)을 따로 둔다. 예전엔 `jets.visible` 하나가 둘을 겸해 "능력이 있다 = 빔이 보인다"가 됐다.
- 분출 연출: 0.62초 동안 `prog 0→1`로 축 방향으로 **뻗어 나가고**(길이 0.30→1.35배) 밝기는 `sin(prog·π)`로 **나타났다 사라진다**. 끝나면 `visible=false`.
- 물리적으로도 방어된다 — 정지 상태의 제트는 플레어보다 수십 배 어두워 실제 관측에서도 outburst 때만 두드러진다.
- 회귀 4건 신설: 퀘이사에서 능력 가용 · 평상시 비가시 · 누른 순간만 분출 · 끝나면 사라짐. 전 회귀 **381/381**.
- 비교 페이지 도식도 평상시 비가시로 갱신.


## 2026-07-30 · 제트 = 쌍극 1회성 축 청소로 확정(4.4)
- 오너 질문: **"제트가 z축 양쪽으로 뿜어져 나오면서 밀어내는 것 아닌가요? 그냥 해당 축의 적들을 밀어내고 사라지게 하는 건? 계속 존재하지 않고 1회성으로."** — 맞다. 제트는 자전축 양쪽으로 대칭(쌍극)이다. 그대로 구현.
- **판정을 매 프레임에서 발사 순간 한 번으로 옮겼다.** 예전 상시 방식은 축 위에 새로 들어오는 것마다 계속 죽어 오너가 "불필요하게 밀어낸다"고 느낀 원인이었다. 지금은 `doFlare()`에서 축 양쪽(`|along|≤reach`)을 한 번 쓸고 끝. 그 뒤에 들어온 천체는 안전하다.
- 물리 구분을 유지: 가스·별은 상대론적 플라스마에 찢겨 날아가지만(질량은 안 들어온다 — 제트는 뿜는 것이므로 점수만), **다른 블랙홀은 밀 수 없다**(사건지평선) → 원반 가스만 날려 강착을 끊고, 굶은 블랙홀은 가스를 찾아 흩어져 추격을 멈춘다.
- 반동은 78 → **54**로 낮췄다. 쌍극 대칭이면 알짜 추력이 0이고, 남는 건 로브 세기 차이 몫뿐이다.
- 실측(`sweptLast()` 계측 훅 신설): 사거리 안 **2개(양쪽)** · 사거리 밖 0 · 안 쏘면 0 · 축 밖 천체 생존 · 발사 뒤 새 천체 안전.
- **측정에서 세 번 헛돌았다** — ① `objInfo()`에 `eaten` 필드가 없어 필터가 무의미했다 ② 천체를 입 안(9.5)에 놓고 재서 제트와 무관하게 먹혔다 ③ `jetWorldTips()`가 사거리(335)가 아니라 10.6을 돌려줬다. 교훈: **간접 지표로 기구를 추정하지 말고 기구 자체를 계측하라.** `sweptLast()`를 만들자 한 번에 끝났다.
- 중괄호를 하나 더 지워 `Unexpected end of input`을 냈고 `git checkout`으로 되돌려 재적용했다. 블록 통째 치환 시 여닫는 짝을 세는 걸 습관화.
- 전 회귀 **377/377**. 비교 페이지(`mock/whats-new.html`)도 확정 설계로 갱신.


## 2026-07-30 · 제트를 '위기 탈출'로 정정 + 절차적 천체 텍스처(4.3)
- **내가 오너 의도를 잘못 읽었다.** "제트를 필요할 때 쓰는 충격파로"를 공격용으로 만들었는데, 오너 의도는 **위기 탈출용**이었다("불필요하게 적을 밀어내는 것 같아서요"). 재설계.
  - **JET ESCAPE (`F` · 모바일 ☄ JET)** — 가장 위험한 상대 쪽으로 한쪽 로브를 터뜨리고 **나는 반대로 튕겨 나간다**. 물리 근거: 대칭 쌍제트는 알짜 추력이 0이지만 실제 제트는 한쪽이 강한 비대칭이 흔하고, 그 비대칭이 블랙홀에 운동량을 준다(jet-driven kick).
  - 반동 78(대시 26의 3배) + 부스트 + **무적 1.2초** + 조석 손아귀 해제.
  - **살상·밀어내기는 전부 제거.** 남긴 건 실제 AGN 피드백(라이벌 원반 가스 박탈)뿐인데, 여기에 하나를 더했다 — **원반이 날아간 라이벌은 추격을 멈춘다**(굶은 블랙홀은 가스를 찾아 흩어진다). 적을 미는 게 아니라 추격을 끊는 것이라 비로소 '탈출'이 된다.
  - 실측: 큰 라이벌 앞에서 **탈출 없이 0/8 생존 → 제트 사용 시 8/8 생존**.
- **천체가 다채롭지 못하다** — 오너가 Higgsfield 사용까지 허락했지만 **절차적 캔버스 텍스처가 이 게임엔 확실히 낫다**고 판단했다. 포털 제출이 단일 HTML이라 PNG를 base64로 심으면 용량이 폭증하고 변형 수도 고정되는데, 절차적이면 **행성마다 전부 다르면서 용량은 0**이다.
  - 무늬는 실제 특징에서: 가스 거인의 **위도별 제트기류 띠 + 대적점**, 지구형의 **대륙·극관·구름**, 얼음 행성의 **유로파식 균열**, 용암 행성의 **갈라진 지각 사이로 새는 마그마**, 암석의 광물 얼룩과 크레이터.
  - 종류마다 7장을 만들어 돌려 쓴다(스폰마다 캔버스를 새로 그리면 낭비).
  - 실측 **60fps 유지**(천체 30개).
- 또 같은 실수를 했다가 스스로 잡았다 — `g.userData.spinAxis=true`를 넣고 쓰지 않았다. 혜성 꼬리와 **똑같은 죽은 선언**이라 바로 제거.
- 전 회귀 **374/374**. `verify-jets`는 새 설계로 재작성(부수지 않는다·탈출로 산다).


## 2026-07-30 · 오너 지적 4건 일괄(4.2)
**① 나선이 시안과 다르고 꺾여 보인다** — 둘 다 내 구현 결함이었다.
- 코어 관은 월드 스케일로 커지는데 **입자는 화면 픽셀 고정**이라, 고질량에서 관이 압도해 E안이 예전 단색 관으로 되돌아갔다. 입자를 `sizeAttenuation:true` + 크기를 본체 크기에 비례로 바꿔 어느 질량에서도 같은 그림이 나오게.
- '꺾임'은 **TubeGeometry의 평평하게 잘린 끝단**이었다. 양 끝을 정점색으로 검게 칠해(가산 혼합에서 검정=보이지 않음) 단면을 지우고, 곡선 제어점 17→23으로 늘렸다.

**② 제트를 필요할 때 쓰는 충격파로**(오너 제안) — 채택. 물리 근거는 **블레이자 플레어**: 제트는 상시 존재하지만 강착이 급증하면 밝기가 수십 배로 치솟는다.
- 제트 빔은 계속 보이되 평상시 불투명도 0.5→0.17·굵기 0.62배로 낮춰 화면을 덜 덮는다.
- **JET FLARE** 능력 신설 — PC `R` · 모바일 `☄ FLARE` 버튼(퀘이사 티어에서만 노출). 에너지 1을 소모해 0.75초간 축을 쓸어버린다. **상시 살상은 폐지.**

**③ 후반이 진부하다** — 먹이 표가 t≥2에서 성단·쌍성 두세 종뿐이었다.
- 신규 2종: **분자운(gascloud)** — 별이 태어나는 차가운 가스 구름, 실제로 블랙홀을 살찌우는 주 연료 · **극대거성(hypergiant)** — R136a1급. 코덱스 등재(31→33항목).
- 색폭 확대: 행성 채도 0.6→0.82, 가스 거인은 실제 색폭(목성 황갈·토성 금·해왕성 청·천왕성 청록)에서 뽑는다.
- 실측 후반 구성: 예전 2~3종 → **7종**(rival·cluster·binary·gascloud·hypergiant·redgiant·pulsar).

**작업 중 내가 만든 심각한 결함 2건 — 둘 다 회귀가 잡았다.**
- `update()` 안에서 `if(S.jetFire<=0)return`을 써서 **제트가 켜진 뒤로는 매 프레임 업데이트 전체가 조기 종료**됐다. 스폰·흡수·티어업이 통째로 멈췄다. 블록으로 감싸 수리. 교훈: **긴 함수 중간의 early return은 그 함수 전체를 끝낸다** — 조건부 스킵은 블록으로.
- `NO_SPAWN=false` 리셋 패치가 **조용히 실패**해 선언부에만 있었다(치환 실패 무검증 — 같은 실수 4번째). 검증용 플래그가 다음 판까지 남는 누수였다. 리셋에 추가.

전 회귀 **372/372**(27개 스위트). 코덱스 수 변경으로 7개 스위트의 상수 갱신.


## 2026-07-29 · 혜성 꼬리 — 코덱스가 가르치던 물리를 게임이 어기고 있었다(4.1.1)
- 오너: **"저 작은 천체의 화살표 같은 건 뭔가요?"** — 혜성 꼬리였다. 그런데 화살표처럼 보인 이유가 코드에 있었다.
- **`userData.tail`을 저장만 하고 어디서도 쓰지 않았다.** 주석엔 "태양풍에 밀려 항상 바깥을 향한다"고 써 있는데 겨누는 코드가 없어, 꼬리는 월드 −Y(화면 아래)로 고정된 채 무작위로 기울고 핵의 자전을 따라 돌았다. **방향에 아무 의미가 없으니 화살표로 읽힌 것이다.**
- 더 나쁜 건 **코덱스가 정확한 사실을 가르치고 있었다는 점** — "The tail always points away from the star, not backwards along its path". 게임이 자기 교재를 어기고 있었다. 이 프로젝트의 최상위 원칙(천체물리 사실이 토대) 위반이다.
- 수리: 꼬리 축을 +X로 만들고 매 프레임 `rotation.y = atan2(-dz/d, dx/d) − 핵의 자전`으로 **광원(내 강착원반) 반대쪽**에 고정. 가까울수록 승화가 활발하므로 길이·밝기를 거리로 스케일.
- 모양도 고쳤다: 단색 원뿔은 회색 깔때기처럼 보였다 → 끝으로 갈수록 **검게 칠한 정점색**(가산 혼합에서 검정 = 보이지 않음)으로 딱딱한 끝단 제거. 처음엔 세로 1단이라 색이 선형으로만 섞여 여전히 잘린 듯 보였고, **10단으로 늘려서야** 곡선으로 흩어졌다.
- 회귀 고정: `cometTails()` 훅 + `verify-lore`에 "꼬리가 광원 반대쪽을 향함"(혜성 6개, 방향 오차 < 0.12). 전 회귀 **366/366**.
- 교훈: **코드 주석이 선언한 물리가 실제로 구현돼 있는지 검사하라.** 주석·코덱스·테스트가 같은 사실을 말해도, 그걸 실행하는 코드가 없으면 게임은 거짓말을 한다.


## 2026-07-29 · 강착 흐름 E안 · 성단 C안 적용(4.1)
- 오너가 시안(`mock/shapes.html`)에서 **흐름 E(코어+입자)** · **성단 C(별 무리)** 를 선택. 둘 다 적용.
- **흐름**: 굵은 관 하나 → **가는 테이퍼 코어**(rem×0.05, 길만 그림) + **잔해 입자 220개**. 곡선에 미세한 흔들림(`sin(u·1.9π + t·1.9)`)을 넣어 고정된 관이 아니게.
  - 구현 중 **두 번 틀렸다**: ① 입자 흐름 방향이 반대였다. `u=1-f; u*=u` 는 원반→본체로 흐르고 원반 쪽에 뭉친다. 물리는 그 반대 — 원반에 가까울수록 빨라지므로 **성글어져야** 한다. `sPar=(1-f)^0.65` 로 고쳐 본체→원반, 밀도 ∝ sPar^0.54(본체 쪽 촘촘·원반 쪽 성김). ② 기본 `PointsMaterial`은 점을 **사각형**으로 그린다 — 확대하면 네모 알갱이가 줄줄이 보였다. 기존 글로우 텍스처(`GT`)를 `map`으로 씌워 해결.
  - 마무리: 옆으로 퍼지는 폭 0.16→0.30(줄이 아니라 흐름으로), 원반 언저리 16% 구간 페이드(딱 끊기지 않고 원반에 스며들게), 입자별 밝기 분산(균일한 밧줄 방지).
- **성단**: 8×6 세그먼트 구 14개 → **점광 320개**, 중심 집중 분포(r=U^2.1), 늙은 별 팔레트(백·황·청). 실제 구상성단은 별 수십만 개다. 예전 것은 실루엣이 팔각형·위아래가 평평해 오너 눈에 **"방향을 나타내는 짧은 원기둥"** 으로 보였다.
- 두 점광 모두 `sizeAttenuation:false` — 질량이 6만 배 커져도 화면상 크기가 유지돼 '입자/별'로 읽힌다(월드 크기로 두면 고질량에서 먼지처럼 사라진다).
- 검증: 고티어(천체 30개) **실측 120fps** — 점광 도입에 따른 성능 저하 없음. 전 회귀 **365/365**.


## 2026-07-29 · verify-proto 불안정성 완전 제거(4.0.5)
- "고쳤다"고 두 번 보고한 뒤에도 병렬 스트레스에서 계속 흔들렸다. 끝까지 파니 **원인이 세 개**였고 전부 제품이 아니라 측정 쪽이었다.
  1. **지형(성운)이 질량을 공급** — `clearField`가 천체만 치우고 성운은 남겨, 플레이어가 성운 안에 있으면 "안 먹으면 감소" 판정에서 오히려 늘었다(6→6.38). → `clearField`가 지형까지 치운다.
  2. **존 배율(zoneLens)이 질량 의존을 가림** — 렌징 세기는 `질량항 × 존 배율`인데 존은 위치·질량에 따라 바뀐다. 두 측정이 다른 존이면 질량이 커도 값이 작아진다. → 존으로 나눠 순수 비교, 라이벌·지형도 제거.
  3. **벽시계 대기가 프레임을 보장하지 않음** — 렌징 유니폼은 렌더에서 갱신되는데 `waitForTimeout(120)`은 부하가 걸리면 프레임을 못 받아 **이전 질량의 값**을 읽었다. → 양쪽 측정 모두 rAF 2회 대기로 통일.
- 성장 판정도 바꿨다: 봇이 난수 필드에서 얼마나 잘 먹느냐(1.0~7.6배로 요동)가 아니라 **먹으면 커지는가**를 통제 필드에서 잰다. 봇 실력이 판정에 끼지 않는다.
- **12회 병렬 스트레스 12/12.** 전 회귀 **365/365**(27개 스위트).
- 교훈: 불안정한 테스트는 "가끔 실패하는 테스트"가 아니라 **아직 원인을 모르는 테스트**다. 세 번 다른 얼굴로 나타났고 셋 다 세계 상태 통제 실패였다.

## 2026-07-29 · 리더보드 격리 재적발 — 조건부 강제의 구멍(4.0.4)
- 결과 화면을 찍다가 제출이 일어나기에 DB를 확인했더니, **테스트 행이 실제 일일 보드에 쌓이고 있었다**(DAILY_QA — 7/27 23건, 7/28 30건, 7/29 13건 = 66건). 7/28에 "환경 경계에서 원천 차단"했다고 기록한 그 조치가 **반쪽이었다.**
- 구멍: `if(DEV)day=day||QA_DAY` — **day가 이미 있으면 덮어쓰지 않는다.** 글로벌 제출(day=null)만 격리됐고, 데일리 제출은 실제 날짜를 들고 오므로 그대로 통과했다.
- 수리: `if(DEV)day=QA_DAY` 로 **조건 없이 강제**하고, `topDaily`도 DEV면 QA 보드를 읽게 해 왕복 검증이 유효하도록 대칭화. daily/lb/meta 스위트 그대로 통과(16·15·23).
- **교훈: "원천 차단했다"는 기록을 믿지 말고 데이터를 봐라.** 차단 코드가 있다는 것과 차단되고 있다는 것은 다르다. 실제 DB를 조회해서야 재적발됐다.
- 남은 청소: 실제 날짜 보드의 DAILY_QA 66건 삭제 — 권한 정책에 막혀 오너 확인 대기.

## 2026-07-29 · 결과 화면 메시지 잔류(4.0.3)
- 게임오버 격자 페이드를 실증하려고 라이브에서 사망 화면을 찍다가 **다른 결함을 발견**: 마일스톤 배너(`◆ CYGNUS X-1`)가 그대로 남아 **EVAPORATED 제목과 겹쳐** 있었다. 우선순위 큐는 배너·코덱스 사이만 중재하고, 결과 패널은 별개 층이라 진행 중인 메시지를 끄지 않았다.
- `gameOver()`에서 큐를 비우고 표시를 내린다. 가로·세로 양쪽에서 잔류 0·겹침 0으로 회귀 고정(`verify-ui` 9/9).
- 교훈: **한 화면을 검증하러 갔다가 옆 화면의 결함이 나온다** — 상태 전이(플레이→결과)는 각 상태를 따로 보는 것만으로는 안 잡힌다.

## 2026-07-29 · 메뉴 십자선 + 흡수 겹침 수리(4.0.2)
- **메뉴의 수직·수평선 = 우주 자(격자)였다.** 오너는 "블랙홀 크기 때문인가" 했지만 아니었다. 실 GPU 재현 → 씬 그래프 프로브(`_probe`) → 오브젝트 하나씩 소거로 특정: `GRID_CELL*4000`(한 변 104,000) 평면 하나를 끄니 두 선이 **동시에** 사라졌다.
  - 원리: 히어로 구도는 카메라가 격자 평면 높이(y≈−1.2)에 놓인다. 그래서 **가로선=평면의 지평선**, **세로선=시선축 격자선이 소실점으로 수렴한 것**. 격자는 3.7에서 성장 체감용으로 넣은 게임플레이 기준자라 메뉴에선 정보가 아니라 잡음이다.
  - 수리: `hero=!S.alive`(이미 있는 구도 판정)와 **같은 조건·같은 속도로 페이드**. 하드 토글은 사망 순간 팝이 생긴다.
  - 덤: 시작점(원점)이 격자 교차점 위라 **플레이 시작 순간에도 화면 정중앙 십자**로 읽혔다 → 텍스처를 반 칸(0.5,0.5) 어긋나게.
- **"천체가 내 블랙홀과 겹쳐 뱅글뱅글 돈다"(오너 리포트) — 실측으로 확정.** 진행 0.19에서 본체 안쪽 끝 **2.65 < 지평선 3.54**. 흡수 중인 천체가 블랙홀 구체를 파고든 채 플레이어를 따라 도는 상태였다.
  - 진짜 원인은 **붙잡는 지점**이었다. 예전엔 '입에 닿아야'(mouth≈1.9hr) 흡수가 시작돼, 시작 순간 이미 원반(2.16hr) 안쪽이었다. 물리적으로도 틀렸다 — **조석 파괴는 로슈 한계, 즉 강착원반보다 한참 바깥에서** 일어난다.
  - 수리 3단: ① 캡처를 **로슈 한계**(`2.16hr*1.06 + 본체반경`)로 이동 ② 유지 거리를 **하드 하한**으로(이징만 두면 빠르게 접근할 때 한 프레임에 파고들어 −0.76 겹침) ③ 차례를 기다리는 다른 큰 천체도 원반 언저리에 세워 둔다(안 그러면 나를 덮은 채 맴돈다).
  - 결과: 흡수 내내 최소 여유 **+1.13**, 원반 침범 0.
- **"빨려오면 한 번에 빨려야"** — 로슈 한계까지 띄우니 잔해 출발점이 멀어져 예전 속도로는 **83%만 가고 사라졌다**. 흡수 완료 잔해만 빠르게(`dt*16`, 0.28s) → 12.4 → **0.06**까지 완주.
- 나선 줄기: 종점을 1.9hr → **2.16hr**(원반 바깥 테두리)로 옮겨 허공에서 끊기지 않게, 길어진 만큼 굵기 하향.
- **흡수 기준은 원래부터 일정하다** — 비-라이벌이고 `내 질량의 60%`(블랙홀 형태) / `50%`(원시성·항성) 이상이면 점진 흡수, 그보다 작으면 즉시. 동시에는 하나만.
- **A/B로 부작용 확인**: 60초 봇 ×7 질량 중앙값 3.33(변경 전) → **5.22**(후). 성장 저하 없음.
- **회귀 5건 신규**(tidal 3 · menu 2) + `verify-proto` **불안정성 제거**: 앞 절에서 죽으면 `step()`이 한 프레임도 안 돌아 뒤 절이 통째로 거짓 통과/실패했다. 성장 판정도 난수 한 판 → **5판 중앙값**으로. 5회 병렬 스트레스 5/5.
- 전 회귀 **363/363**(27개 스위트).


## 2026-07-29 · 메시지 우선순위 큐 + 낡은 회귀 3건 수리(4.0.1)
- **근본 원인 제거**: 배너·코덱스가 각자 독립 발화해 좁은 화면에서 겹쳤다. 이제 `MSGQ` 우선순위 큐가 **한 번에 하나만** 띄우고 나머지는 줄 세운다. 우선순위 3=전이/스킨 해금, 2=일반 배너, 1=코덱스 발견 — 더 중요한 게 오면 낮은 건 즉시 교체된다. 중복 키는 큐에 안 들어가고, 백로그는 4개에서 잘린다(오래 밀린 저순위 메시지가 뒤늦게 튀는 것 방지).
- **발견은 항상 기록, 표시만 큐가 결정** — 코덱스 수집률이 연출 타이밍에 영향받지 않는다.
- 검증: 세로(393×852)에서 60회 성장·스폰을 몰아치며 표본 채집 → **배너+코덱스 동시 표시 0회**(이전 3채널 동시). `verify-ui.mjs`에 회귀로 승격(7/7).
- **`verify-proto`가 오래 빨간 채로 방치돼 있었다(7/10)** — 상시 빨간 스위트는 회귀 감지를 무디게 한다. 3건 모두 **설계 변경을 못 따라간 낡은 단언**이었지 버그가 아니었다:
  - `SUPERMASSIVE @ 질량 60` → 현행 티어표에서 SUPERMASSIVE는 **1200+**, 60은 STELLAR-MASS.
  - `렌징이 질량 따라 안 커짐` → **측정 결함**. 렌징 유니폼은 렌더에서 갱신되는데 프레임을 안 기다리고 읽었다. 실제로는 0.0039 → 0.11로 **28배** 정상 스케일.
  - `안 먹으면 질량 감소` → **하네스 결함**. 타겟만 멀리 뒀지 스포너가 계속 먹이를 채웠고 중력이 그걸 끌어와 6 → 8.78로 **오히려 늘었다**. 검증 전용 `setSpawn(false)`·`clearField()` 훅으로 유입을 끊고서야 순수 손실이 보인다.
  - 교훈은 이전 nova-surge 건과 같다 — **미검증 하네스의 실패는 제품 결함이 아닐 수 있다. 하네스부터 의심한다.**
- 전 회귀 **357/357**(27개 스위트, proto 10/10 복구 포함).


## 2026-07-29 · 나선 흐름 확정 + UI 간섭 전수 감사(4.0)
- **나선 흐름 채택**(오너 선택). 처음엔 굽이가 안 보였다 — 원인은 **본체를 지평선 바로 옆에 붙여 둬 휠 거리가 없었던 것**. 실제 조석 박리는 **로슈 한계**(지평선 밖 여러 반경)에서 일어나므로 이격 거리를 `rad*0.95` → **`rad*1.75`** 로 늘리고, 끝점을 중심이 아니라 **접선 방향으로 어긋난 원반 언저리**에 뒀다. 실제 강착 흐름이 원반을 '감고' 합류하는 모양 그대로다. 색도 원반(따뜻함)과 대비되는 청백으로.
- **UI 감사를 다시 지었다**. 기존 감사는 두 번 뚫렸다(글자 없는 서지 바 / 대상 밖이던 음소거 버튼). 새 기준은 **"그리는 모든 것"** — 글자·배경·테두리·그림자 중 하나라도 있으면 대상. 6개 뷰포트(가로·세로·태블릿 포함)에서 **모든 메시지 채널을 동시에 띄운 최악의 경우**로 잰다.
  - **감사 훅(`forceUI`)이 필요했다**: `hud()`가 매 프레임 강제 표시를 되돌려, 처음엔 감사가 **요소 2개만 세고 통과**하고 있었다(아무것도 못 재는 상태로 초록). 타이머를 미래로 미는 훅을 넣고서야 실제로 **18건**이 드러났다.
- **적발·수리 18건 → 0건**: 음소거 ⨯ WAVE 버튼(내가 클러스터를 우하단에 놓아 생김) · 좁은 화면에서 상단 HUD ⨯ 질량바 · 인트로 카드 ⨯ HUD(→ 인트로는 모달이므로 HUD를 숨김) · 음소거 ⨯ 버전표기 · 세로에서 질량바 ⨯ 콤보 · 코덱스 ⨯ 음소거.
  - 세로 모드는 하단이 코덱스·능력으로 붐벼 **질량바를 상단 HUD 아래 자기 줄**로 옮겼다.
- `verify-ui.mjs`로 **상시 회귀 승격**(6/6). 전 회귀 **346/346**.
- **남은 구조적 관찰**: 세로 모드에서 메시지 채널이 **동시에 3개**(배너·코덱스·콤보) 뜬다. 지금은 위치로 피했지만, 근본은 **채널마다 독립 발화하고 중재가 없다**는 것 — 우선순위 큐가 다음 과제다.


## 2026-07-29 · 흡수 연출을 '늘어남'에서 '뜯겨 나옴'으로(3.9)
- 오너: **"저건 단순히 늘어나는 거고, 원래는 천체를 조금씩 문어발처럼 빨아들이듯 보여야 하지 않나?"** — 맞다. 3안(타원체/눈물방울/실 가닥)을 만들어 비교안까지 냈지만 **셋 다 천체 자체를 변형**하는 접근이었다. 실제 강착은 본체가 늘어나는 게 아니라 **표면에서 물질이 벗겨져 좁은 흐름으로 떨어지는 것**이다.
- **재설계**: 본체는 형태를 유지한 채 줄어들고(조석 팽대는 1.28배까지만), **천체 표면 → 블랙홀을 잇는 흐름 줄기**(원뿔 메시, 천체 쪽이 굵고 블랙홀 쪽이 가늘다 + 정점색으로 밝기 구배)가 생긴다. 줄기를 따라 흐름 입자가 계속 흘러든다.
- 실측: 진행에 따라 줄기 길이 3.8 → 11.1, 불투명도 0.31 → 0.54. 완료·취소 시 즉시 숨김.
- **교훈**: 3안 비교까지 갔는데 **전제가 틀려 셋 다 오답**이었다. 오너의 "원래는 …아닌가요?"가 전제를 깼다. 선택지를 잘 만드는 것보다 **전제가 맞는지 먼저 묻는 게** 빠르다.
- 부수: 인트로 "press any key / tap to begin" 문구를 8.5px 회색 → **11px 주황 + 깜빡임**으로.
- 전 회귀 **340/340**(verify-menu·verify-lb는 배치 중 각 1회 흔들림, 단독 3회 통과).


## 2026-07-29 · 인트로 동안 게임을 멈춘다(3.8.2)
- 오너: "게임이 가이드와 동시에 시작돼 읽을 여유가 없다." 카드에는 **"press any key to begin"** 이라 적혀 있는데 정작 게임은 이미 돌고 있었다 — 문구와 동작이 어긋나 있었다.
- 수정: 카드가 떠 있는 동안 `frame()`에서 `update`/`updateCore`를 건너뛴다. **입력이 오면 그때 시작**하고, 자동 해제 9초는 안전장치로만 남긴다. 실측: 카드 중 1.8초 동안 게임 시간 0·질량 1.2 그대로, 키 입력 후 흐르기 시작.
- `frame()`에서만 막았다 — DEV `step()`은 `update`를 직접 부르므로 **340개 검증이 영향받지 않는다**.
- **작업 중 두 번 깨뜨렸다**: 주석을 줄 끝에 붙였다가 그 줄에 있던 **함수 닫는 `}`를 주석 처리**해 부팅 불가(`Unexpected end of input`), 고치면서 `}`를 하나 더 넣어 또 실패. **한 줄에 코드와 닫는 괄호가 같이 있으면 줄 끝 주석은 금지.**
- 전 회귀 **340/340**.


## 2026-07-29 · 모바일 조작을 오른쪽으로 + 겹침 감사의 구멍 두 개(3.8.1)
- 오너가 **실기 스크린샷**으로 두 가지를 지적: ① 코덱스 문구 카드가 하단 UI와 겹친다 ② 모바일은 버튼이 오른쪽에 있어야 하지 않나.
- **② 맞다**: 이동은 왼쪽 조이스틱이므로 능력은 **오른손 엄지 사정권**이어야 한다. `body.touch`에서 `#surge`를 **우하단 세로 클러스터**(DASH/SPIN/WAVE)로 옮기고, 코덱스 토스트와 흡수 바는 **좌하단**으로 분리. 실측 3개 모바일 크기에서 실제 3줄 문구로도 겹침 0.
- **① 왜 자동 감사가 못 잡았나 — 구멍이 둘이었다**:
  - **(a) 글자 없는 요소를 안 봤다.** 문구를 덮은 건 텍스트가 아니라 **서지 바**였는데, 감사가 '텍스트 리프'만 대상으로 삼았다. → **배경·테두리를 가진 판**까지 포함하도록 확대(전면 오버레이는 '덮는 게 목적'이라 제외).
  - **(b) `#mute` 가 아예 대상 밖이었다.** 확대 후 실측하니 음소거 버튼(814–836)과 `CHARGING` 라벨(758–838)이 **실제로 겹치고 있었다.** → 감사에 명시 포함 목록(`FORCE`)을 두고, 터치에서는 서지 라벨을 숨겨 그 자리를 음소거 버튼에 넘김.
- **교훈**: 감사가 통과했다고 안전한 게 아니라, **감사의 대상 정의가 곧 사각지대**다. 오너의 실기 스크린샷 한 장이 자동 검사 3회분을 이겼다.
- 전 회귀 **340/340**, 겹침 감사 3/3(대상 확대 후).


## 2026-07-29 · 티어별 주민 + 흡수 단축(3.8) — 개선안 B·F 완료
- **B · 티어별 먹이 교체**: 실측에서 **질량 6만 배 동안 천체 종류가 6종 고정**이었다(새 콘텐츠 0). 실루엣이 다른 **6종 신규** — 혜성(핵+긴 꼬리) · 가스 거인(고리) · 적색거성(크고 어두움) · 백색왜성(작고 지독히 밝음) · **쌍성계**(둘이 서로 공전) · **성단**(14개 무리). 색이 아니라 **실루엣**을 갈랐다.
  - 티어별 주민 표: 원시별=암석·혜성 / 주계열성=암석·행성·가스 거인·항성 / 항성질량=별의 시체들 / 중간질량=쌍성계 등장 / 초대질량 이상=**성단이 주식**. 실측 총 11종이 등장하고 **구간마다 얼굴이 바뀐다**.
  - 코덱스 **31항목**(+6): 혜성 꼬리는 진행 반대가 아니라 항상 별 반대쪽 · 목성은 지구 318개지만 태양의 1/1000 · 백색왜성 한 스푼 1톤 · 별의 절반 이상은 홀로가 아니다 등.
- **F · 흡수 지속 25% 단축**(오너 판단 요청에 대한 답: **유지하되 짧게**). 시간이 걸리는 것은 후반의 유일한 긴장 장치라 없애면 더 단조로워진다.
- **버그**: 스폰 분기 치환이 한 번 빗나가 새 천체가 하나도 안 나왔다(옛 `type=rel<1.5?...`가 뒤에 남아 덮어씀). 실측으로 잡음 — **치환 후에는 반드시 결과를 세어 확인할 것**(같은 실수 3번째).
- `verify-spawn`의 "초반에 행성이 나온다"는 이번 설계로 의도가 바뀌었다(원시별은 먼지·혜성을 먹는다) → **"초반에 먹이가 충분히 나온다"** 로 갱신.
- 전 회귀 **340/340**. 개선안 6항목(A~F) 전부 완료.


## 2026-07-28 · 성장 체감 — 원인을 측정하고 구조를 바꿈(3.7)
- 오너 "어느 순간부터 커지는 느낌이 없다". **측정으로 원인이 한 줄로 나왔다**: 질량 2 → 120,000(6만 배) 동안 **먹이/나 화면 비율이 0.40 → 0.54로 오히려 커진다.** 크기를 판단할 기준이 '같이 커지는 먹이'뿐이라 성장은 **원리적으로 체감될 수 없는 구조**였다. 천체 종류도 **6종에서 고정** — 6만 배 자라는 동안 새 콘텐츠가 0.
- **D · 질량 비교 마일스톤**(비용 최저): 태양 → Cygnus X-1 → R136a1 → 중간질량 → 성단 중심 → 왜소은하 → **Sgr A*(430만)** → M87*(65억) → TON 618(660억). 9단계를 넘을 때마다 배너.
- **A · 우주 자**(근본 해법): 월드 좌표에 **고정된 격자**(한 칸 26단위). 카메라가 물러날수록 칸이 촘촘해져 성장이 눈으로 읽힌다 — 실측 **1.1칸 → 13.2칸(12배)**. 별·성운은 `bgScene`에 있어 카메라를 따라다니므로 기준자가 되지 못했다.
  - 처음엔 불투명도 0.16 + 밉맵이라 **화면에서 아예 안 보였다**. 밉맵을 끄고(멀어질수록 선이 사라진다) 선 굵기·불투명도를 올려 해결.
- **E · 제트 축 고정 + 정렬**(오너 지적 "원래 멈춰 있지 않나요?" — 맞다): 세차 회전을 걷어내고 축을 고정. 조준은 **바딘–패터슨 효과**로 — 강착 원반의 토크가 자전축을 원반 각운동량 쪽으로 정렬시킨다. **먹은 방향이 축을 끌어당긴다.** 실측: 오른쪽에서만 먹으면 축 1.57 → 0.00, 위쪽에서 먹으면 1.57 유지.
- **C · 은하 중심**(후반 목표): 크기가 **고정된 유일한 천체**(Sgr A*와 같은 430만). 화면 점유가 **2090% → 137%** 로 줄어들며, 질량이 그 55%를 넘으면 삼킬 수 있고 그 전에 닿으면 죽는다.
- 신규 `verify-growth.mjs` **11/11**, 전 회귀 **329/329**.
- 남은 처방: **B**(티어별 먹이 교체 — 비용 최고) · **F**(흡수 지속 단축).


## 2026-07-28 · 모바일 조작 실측 교정(3.6.1) — "다 끝났나" 물음에 점검하다 나온 것
- 오너 "모바일 확인 외에는 다 마무리된 거죠?" → 실기 없이 확인 가능한 것을 훑다가 **3건 발견**.
- **① 모바일 온보딩 문구가 실제 버튼과 어긋났다**: 능력을 2개→3개로 늘리면서 카드의 모바일 분기를 안 맞춰, 3번째 줄이 없어진 이름 `PULSE`, 4번째 줄은 키보드 키 `E`가 그대로 떴다. 화면 버튼명과 1:1로 교정.
- **② 이동(드래그)과 버튼명 `DRAG`가 겹쳐 헷갈렸다** → 프레임 드래깅 버튼을 **`SPIN`**(시공간을 끌어 돌린다)으로 개명.
- **③ 능력 버튼이 23px** — 엄지 탭 권장치(44px)의 절반이었다. `body.touch` 클래스로 **78×44px** 확보(데스크톱은 그대로).
  - 두 번 헛발을 짚었다: (a) `@media (pointer:coarse)`는 **에뮬레이션에서 안 잡혀** 검증이 불가능했다 → 이미 있던 JS 판정(`COARSE`)으로 클래스를 붙이는 방식으로 교체. (b) CSS를 **여러 줄 규칙 블록 안쪽에 삽입**해 무효가 됐다 — 규칙 밖으로 옮겨 해결.
- 전 회귀 **329/329**, 겹침 감사 3/3.
- **교훈**: "검증할 수 없는 수정은 하지 않는다." 미디어 쿼리처럼 헤드리스에서 재현 안 되는 조건은 JS 판정으로 바꿔야 회귀가 지켜준다.


## 2026-07-28 · 능력 3종을 실제 현상 위에 다시 세움(3.6)
- **기존 능력의 물리 근거가 둘 다 약했다**: DASH는 "중력 슬링샷"이라 했는데 **블랙홀이 자기 자신을 슬링샷할 수는 없다**. PULSE는 근거 없는 범용 중력 폭발(끌어당김+밀어냄을 한 버튼에 섞어 둠).
- **재설계 — 각각 실재하는 현상 하나씩**:
  | 키 / 버튼 | 능력 | 실제 근거 |
  |---|---|---|
  | SPACE / DASH | **RECOIL KICK** | 비대칭 중력파 방출로 병합 잔해가 최대 수천 km/s로 걷어차인다 — 블랙홀이 스스로 가속하는 **유일한 실제 경로** |
  | SHIFT / DRAG | **FRAME DRAGGING** | 회전하는 블랙홀이 시공간을 끌어 주변 물질을 **강제로 공전**시킨다(Lense-Thirring). 밀어내는 기능은 떼어내고 **끌어들임 + 접선 속도**만 남김 |
  | E / WAVE | **GRAVITATIONAL WAVE**(신규) | 병합은 시공간 파동으로 에너지를 방출하고 궤도를 흐트러뜨린다. 위협을 밀어내고 **라이벌의 강착을 1.6초 끊는다** |
- 셋 다 같은 에너지 바를 소모 → **하나를 고르는 선택**이 유지된다.
- 코덱스 2항목 추가(**25항목**): GRAVITATIONAL WAVES(LIGO 2015 · 13억 광년), FRAME DRAGGING.
- **버그**: 파동을 속도 임펄스로만 주니 **중력 흡입이 즉시 상쇄**했다(흡입은 위치를 직접 옮기는데 파동은 속도만 줬다) → 파면이 지나간 만큼 **실제 변위**를 주고 짧은 재포획 쿨다운을 붙임.
- 신규 `verify-abilities.mjs` **12/12**(에너지 게이트 · RECOIL 이동거리 1.25배 · DRAG 흡인 72→12 · WAVE 밀어냄·강착 차단·파면·코덱스 LIGO 문구). 전 회귀 **329/329**.


## 2026-07-28 · 마우스 조종 제거 · 인게임 온보딩 · 조석 박리 + 중력 경쟁(3.5)
- **마우스 조종 제거**: 오너 "게임을 시작하면 커서를 인식해 천체가 그쪽으로 움직인다" — `mousemove`가 즉시 목표를 잡아 의도치 않게 끌려갔다. 리스너 제거. 조작은 **키보드(WASD·방향키) + 모바일 조이스틱**뿐. `setMouse`는 DEV/자동화용으로만 남긴다.
- **인게임 온보딩**: `begin()` 직후 조작 카드(2.6초, 첫 입력 시 즉시 닫힘). 모바일은 DRAG/DASH/PULSE로 문구가 바뀐다.
  - 인트로 중에는 **배너·팝업·코덱스 토스트를 억제**한다(겹침 + 조작 안내 중복). 단 **코덱스 발견 기록은 계속** — 처음엔 가드를 `discover` 앞에 둬서 발견까지 막혔고 `verify-codex` 15/19로 잡혔다.
- **조석 박리(구간별 흡수)**: 로슈 한계 안쪽에서는 접촉 전에도 바깥층이 벗겨진다. 흡입권 안에서 근접도^2.6에 비례해 **초당 질량을 조금씩 이전**하고 흐름 입자를 흘린다. 실측 먹이 200 → 191(접촉 전).
- **중력 경쟁**: 같은 먹이를 두고 누가 가져가는지는 그 지점의 **중력 세기 M/r²**로 갈린다(조석 지배). 프레임마다 각 천체에 대해 '가장 센 라이벌'을 미리 구해 두고, 나와 라이벌 양쪽이 이 비교를 존중한다. 실측: 더 큰 라이벌 옆 먹이는 내가 못 가져감(내 0.104 vs 라이벌 0.669).
- **밸런스 영향 A/B**: 성장 20·400 구간 변화 없음, **6000 구간 33.6% → 45.4%**(후반 정체가 더 풀림). 점수율 33.6k/s로 안티치트 여유 유지.
- 전 회귀 **317/317**.


## 2026-07-28 · 제트는 뿜는 것이다 — 방향 정정 + AGN 피드백(3.4)
- 오너: **"제트는 빨아들이는 게 아니라 분출하는 것 아닌가요?"** → 맞다. 상대론적 제트는 물질을 광속에 가깝게 **뿜어내고**, 제트 자체가 블랙홀을 살찌우지 않는다(제트를 밝히는 에너지원은 강착이다). 내가 넣은 "제트가 훑으면 흡수"는 **방향이 정반대**였다.
- **수정**: 빔에 걸린 천체는 **바깥으로 날아가며 부서진다**(속도 +46, 질량 ×0.42). **질량은 들어오지 않고** 그때 풀려난 에너지가 **점수·서지**가 된다. 실측 3.5초 동안 질량 +314(제트가 아니라 일반 포식분), 점수만 상승.
- **점수 균형**: 처음엔 `mass*9`로 줬더니 폭파가 포식보다 **5배 후해** 앞뒤가 안 맞았고 점수율이 881k/s로 폭주했다 → **"먹었다면 얻었을 점수의 35%"** 로 묶어 34k/s(안티치트 120k 대비 3.5배 여유).
- **큰 라이벌**: 밀어내던 것(제트가 사건지평선을 밀 수는 없다) → **AGN 피드백**으로 교체. 제트가 라이벌의 강착원반 가스를 쓸어내 **일시적으로 못 먹게** 한다(원반이 흐려지는 시각 신호 + `DISK STRIPPED` 배너). 실측: 먹이 6개 옆에서도 질량 120,012 그대로.
- **버그**: 폭파 쿨다운을 루프 맨 위에 두니 **라이벌 스턴 갱신까지 막혀** 견제가 끊겼다 → 스턴 분기 뒤로 이동.
- 테스트도 전제를 바꿨다: '먹혔는가'(`tagEaten`) → **'제트에 맞아 부서졌는가'**(질량 급감). `verify-jets` 15/15.
- 전 회귀 **317/317**. `verify-eat`의 성장 단정도 5회 중앙값으로(4번째·마지막 초반 분산 하네스 수리).


## 2026-07-28 · 거대 천체가 플레이어를 덮던 버그 + 커서·시작 안내(3.3)
- 오너: "큰 행성을 먹으면 내 위치에 중첩되어 늘어난 채 유지되다가 사라진다".
- **원인은 소유권 충돌**: 흡수 중인 천체의 위치·형태를 **세 곳이 동시에 건드렸다** — `updateFeed`(중심으로 lerp + 균일 축소) · 중력 흡입(중심으로 가속) · 조석 늘어남(스케일 덮어쓰기). 계측 결과 천체가 **플레이어 중심(거리 0.5)** 까지 끌려오고 초반 크기 14가 **내 반경 10보다 커서** 2.7초간 나를 덮었다.
- **수정**: 흡수 중인 천체는 `updateFeed`가 **위치·형태를 단독 소유**한다(흡입·늘어남 모두 건너뜀). 그리고 중심이 아니라 **내 표면 바깥(내 반경×0.95 + 남은 반경/2)** 에 붙여 조석 방향으로 늘이며 깎는다 — 실제 조석파괴는 가장자리에서 뜯겨 나가는 현상이다. 실측 최소 간극 **+0.60 / +0.29**(1.9배·0.8배 천체), 이전엔 −9.87.
- **커서**: `crosshair` → `default`. 조준하는 게임이 아닌데 십자선은 오해를 준다. **드래그 조종은 유지** — 터치와 같은 코드 경로이고 데스크톱 사용자가 반사적으로 드래그한다.
- **시작 조작 안내**: 첫 힌트(`◆ MOVE`)가 `onboarded`면 아예 안 뜨던 것 → **이동 안내만 매판 노출**, 나머지 온보딩은 첫 플레이에만. 타이틀의 키 안내도 8px·#3F444B → 9.5px·#7E848C로 읽히게.
- 전 회귀 **315/315**. `verify-depth`의 "힌트는 반복되지 않는다"는 이번 요청으로 의도가 바뀌어 "**이동 안내만 반복**"으로 갱신.


## 2026-07-28 · 포털 SDK 어댑터 이식(3.2) — 제출 선결 조건
- 감사 결과 ACCRETION은 **단일 파일 190KB·외부 스크립트 의존 0건**(Three.js·폰트 전부 인라인, 외부 통신은 Supabase 리더보드뿐)이라 포털 요건 대부분을 이미 만족했으나 **포털 SDK가 없었다**. CrazyGames·Poki 모두 사실상 필수.
- nova-surge에서 검증된 계약을 **바닐라 JS로 이식**: `pickPortal(host,search)` → cg/poki/local, `init·loadingStart/Stop·gameplayStart/Stop·happytime·midgameAd·rewardedAd`.
- **계약의 핵심은 "어떤 실패에도 게임을 막지 않는다"**: SDK 로드 6s 타임아웃 · 광고 워치독(미드게임 15s·리워디드 20s) · 콜백 1회 보장(중복 호출 차단) · 전 메서드 try 격리 · 광고 중 **강제 뮤트 후 원래 상태로 복원**(사이트 뮤트 우선권).
- CG **Progress Save(Data Module)**: 8개 키를 1회 이관 후 `localStorage` get/set을 래핑해 **SDK 우선·로컬 미러 이중화**. nova-surge 실측에서 비로그인/프리뷰는 SDK 쓰기가 무효였기 때문.
- 연동 지점: 첫 프레임 후 `loadingStop` · `begin()`에서 `gameplayStart` · `gameOver()`에서 `gameplayStop` · **AGAIN 버튼에서만 미드게임 광고**(자연스러운 휴지점, 쿨다운 120s) · 점화 마일스톤에서 `happytime`.
- 신규 `verify-portal.mjs` **12/12**: 호스트 판별 4종(`?portal=` 강제 포함) · 로컬 폴백 · 첫 프레임 후 로딩 완료 · **SDK 없어도 광고 콜백 보장** · 리워디드는 광고 없으면 보상 없음 · **광고 후 오디오 게인 복원(0.85 → 0.85 실측)** · 시작/종료 경로 무결.
- 전 회귀 **315/315**.
- **남은 제출 준비**: 30초 플레이 영상, 포털별 규격 크롭, SUBMISSION.md(복붙 메타데이터), 그리고 오너의 모바일 실기 테스트.


## 2026-07-28 · 적응형 품질 — 저사양 안전망(3.1)
- 배경: 2.9의 **강착 흐름 파티클**이 저사양 기기에서 프레임을 먹을 수 있는데, 오너의 모바일 테스트는 2.8 기준이라 **검증 공백**이 있었다. 실기 없이 막을 수 있는 방법으로 안전망을 넣음.
- 구현: 렌더 루프에서 실측 FPS를 1.2초 창으로 재고 **42fps 미만이면 저사양 모드**(복귀는 54fps 초과 — 이력 hysteresis로 경계 깜빡임 방지). 저사양에선 ① 파티클 상한 **140 → 45** ② 강착 흐름은 **아주 가까운 천체(st≥0.62)에서만**, 방출 간격 0.07 → 0.2s.
- **검증**: 모바일 에뮬레이션(844×390)에서 **자동으로 켜짐 — 26fps 감지 → 상한 45**. 데스크톱에선 60fps로 정상 모드 유지. `__acc.fx()`/`setFxLow()` 훅으로 강제 전환 검증도 가능.
- 회귀 중 엣지 함수 CORS 실패 1회 → 직접 확인 결과 `access-control-allow-origin: *` 정상, **배치 부하로 인한 일시 실패**(재실행 3회 통과).
- 전 회귀 **303/303**.


## 2026-07-28 · [중대] 자동 검증이 라이브 리더보드에 계속 쓰고 있었다(3.0.2)
- 3.0.1에서 `verify-lb` 하나만 격리했는데, **여러 스위트가 각자 제출하고 있었다** — DB 실측: `QA_PILOT`·`WEEK_QA`·`DAILY_QA` 가 글로벌·주간·데일리 보드에 누적. 개별로 막는 방식이 애초에 틀렸다.
- **근본 수리**: `LB.submit`에서 **DEV 모드(`?dev=1`)의 모든 제출을 QA 전용 날짜 보드(2000-01-01)로 강제**. 자동 검증은 전부 DEV로 도므로 한 줄로 전 경로가 막힌다. 실측: 수리 후 전 회귀를 돌려도 **글로벌 신규 0건**, QA 보드로만 13건.
- 누적 오염 정리: 글로벌에서 QA 계열 전량 삭제 → **실제 기록 98행**만 남음(`ANON`은 실플레이일 수 있어 손대지 않음).
- 단정 2건 재설계: "내 행이 주간 보드에 뜬다"는 **프로덕션에 써야만 성립**하는 검사였다 → 주간 보드가 ①행을 돌려주고 ②점수순이며 ③**QA가 새지 않는지**를 본다. 회귀가 오염을 상시 감시한다.
- 모바일 가로 BEGIN 탭 영역 36px → **46px**(권장 44px 충족).
- 전 회귀 **303/303**.
- **교훈**: 테스트가 프로덕션에 쓰는 문제는 **개별 호출을 막지 말고 환경 경계에서 막아야 한다.** 한 곳을 고쳐도 다른 스위트가 계속 쓴다.


## 2026-07-28 · 자체 감사에서 나온 잔여 이슈 2건 수리(3.0.1)
- 오너: "보완할 점, 누락된 점, 이슈 없나요?" → 스스로 감사해 두 건을 수리.
- **① 모바일 가로에서 BEGIN 탭 영역 36px** — 권장 44px 미달이었다(2.8 모바일 감사에서 발견해 두고 미수리). `min-height:46px` 적용, 가로·세로 모두 **46px** 확인.
- **② `verify-lb`가 여전히 글로벌 보드에 1행/실행을 썼다** — 2.7.1에서 XSS 검사만 격리하고 유효 제출은 남겨 뒀던 것. `submitRun`에 `day` 인자를 열어 QA는 **2000-01-01 보드로만** 제출하게 격리(글로벌 집계는 `day IS NULL`만 보므로 절대 안 뜬다). 그 사이 쌓인 QA행 **19건 삭제**(글로벌 123행 유지).
  - 단정도 반대로 뒤집었다: "보드에 QA 이름이 있다" → **"QA 제출이 글로벌을 오염시키지 않는다"**. 회귀가 오염을 감시한다.
- 전 회귀 **301/301**.


## 2026-07-28 · 블랙홀 진입 페이싱 + 플레이 요소 인지(3.0)
- 오너: "블랙홀 단계 진입이 다소 빠르다" / "게임 내 여러 재미 요소를 알 수 있으면 좋겠다".
- **① 페이싱**: 임계값 20 M☉는 물리적으로 맞으므로 건드리지 않고, **형태별 강착 효율**을 도입해 주계열성만 `acc:0.68`. 근거는 실제 현상 — **무거운 별은 복사압과 항성풍으로 유입 가스를 밀어내** 강착이 비효율적이다. 실측 A/B: 주계열성 구간 1.00s → **1.40s**, 블랙홀 도달 1.50s → **1.90s**(2.5 이전 원본 1.20s 대비 1.58배).
- **② 인지 장치**: 이미 있는데 한 번도 설명되지 않던 두 요소에 **최초 1회 배너** 추가 — `◈ GRAZE`(큰 블랙홀을 스칠수록 에너지), `☄ RELATIVISTIC JETS`(제트가 점화되는 순간·크기 제한 없음). 조석 영역·병합·초신성·점화는 이미 배너가 있었다.
- 전 회귀 **301/301**(verify-lategame은 라이벌 표본 흔들림 — 재실행 3회 통과).
- **교훈(대화 운영)**: "컨텍스트가 소진돼 새 세션에서" 라고 안내했으나 **틀렸다** — 대화는 자동 요약으로 이어지므로 중단할 이유가 없다. 오너 지적으로 정정하고 계속 진행.


## 2026-07-28 · 강착 흐름 — 천체가 '늘어나며' 빨려든다(2.9)
- 오너: "블랙홀이 모든 걸 한순간에 빨아들이나? 영향권에 들어오면 조금씩 흡수되는 게 현실적이다."
- **실제 물리 그대로**: 조석력은 1/r³로 커지므로 천체는 블랙홀 방향으로 **늘어나(spaghettification)** 가느다란 **강착 흐름**으로 찢겨 나선을 그리며 떨어진다.
- 구현(순수 연출 — 밸런스 무변경): 흡입권 안에서 `st=(1-d/pull)^1.5` 로 ① 천체를 블랙홀 축 방향으로 **최대 2.35배 늘이고 옆으로 눌러** 조석 변형을 만들고 ② 표면에서 **흐름 입자**를 떼어내 접선 성분(각운동량)을 줘 **나선을 그리며** 빨려들게 했다. 영향권을 벗어나면 원래 형태로 복원.
- 파티클은 기존 풀(`particles`/`getP`) 재사용 + 상한 140으로 프레임 보호.
- 전 회귀 **301/301**(verify-daily는 초반 고분산 흔들림 — 재실행 3회 통과).
- **남은 미결**: ① 블랙홀 진입 페이싱(주계열성 흡수 효율 하향 — 무거운 별의 복사압·항성풍으로 정당화) ② 플레이 다양성 인지 장치(첫 조우 안내).


## 2026-07-28 · 제트 회전은 물리 오류였다 + 후반 화면 정리(2.8.1)
- 오너 질문: "퀘이사가 빙글빙글 도는 게 천체물리학적으로 맞나?" → **아니다. 명백한 오류였다.** 상대론적 제트는 블랙홀 **자전축**을 따라 뿜어지고 축은 안정적이다. 등대처럼 도는 건 **펄서**의 특징이지 퀘이사가 아니다. 다만 **세차운동(Lense-Thirring)** 은 실재한다 — SS 433은 약 162일 주기.
  - 수정: 회전 `dt*0.75` → **`dt*0.16`**(세차 수준). 게임성상 축이 완전히 고정되면 제트가 무용지물이라 '느린 세차'로 절충 — 이건 실제 현상이므로 논리적 오류가 아니다.
  - 코덱스 QUASAR 사실도 정정: "제트는 자전축을 따라 광속에 가깝게 뿜어지고, 축은 몇 달에 걸쳐 아주 천천히 세차할 뿐이다."
- 오너: "블랙홀이 커지면 천체가 너무 많아 복잡하다" → 후반 개수 상한을 질량에 따라 낮춤. 실측 **56 → 30개**(질량 5만). 후반엔 천체 하나가 커지므로 개수를 줄여도 밀도 체감은 유지된다.
- 전 회귀 **301/301**(verify-lb는 리더보드 네트워크 지연으로 1회 흔들림, 재실행 통과).
- **미결(오너 리포트 · 다음 블록)**: ① 블랙홀 단계 진입이 다소 빠름 ② 흡수가 '한순간'이라 비현실적 — 영향권에 들어오면 **강착 흐름(accretion stream)** 으로 조금씩 빨려드는 표현 ③ 다양한 플레이 요소를 플레이어가 인지하게 하는 장치.


## 2026-07-27 · 중·후반 미술 3건 수리(2.8) — 스토어 자산 시안에서 적발한 것
- **① 라이벌 블랙홀이 사탕 행성처럼 보이던 문제**: 가독성 색(초록=먹을 수 있음/빨강=위협)을 **강착원반에 칠하고 있었다** → 민트색 고리를 두른 파스텔 행성이 됨. 수정: **원반은 언제나 뜨거운 강착원반**(블랙홀 정체성)으로 고정하고, **신호는 바깥 글로우**가 담당(초록/빨강). 후광(halo)만으로는 부족 — 후광은 화면상 크기가 고정이라 큰 라이벌에서 약하다.
- **② 제트가 납작한 흰 판**: 정점색으로 길이·둘레 방향 감쇠(뿌리 백열 → 끝 청암, 중심 → 가장자리 어둡게) + 불투명도 .5. 가산 합성이 흰색으로 포화되던 것을 완화해 형태가 생겼다.
- **③ 고질량에서 천체가 화면을 잠식 — 캡처 아티팩트가 아니라 진짜 문제였다**: 자연 스폰만으로도 화면이 파스텔 행성에 덮인다. 2.7에서 올린 스폰 상한 4.0 → **2.8배**로 되돌림.
- **가독성 회귀를 테스트가 잡았다**: `verify-readability`의 "먹을 수 있는 라이벌은 초록"이 원반 색을 보고 있어 정당하게 실패. 판정 대상을 **글로우**로 옮기고, "**원반은 둘 다 뜨겁다**(라이벌이 블랙홀로 읽힌다)" 단정을 새로 추가 — 이 수리가 되돌아가면 걸린다. `rivalColors` 훅에 glow/halo 추가.
- 흔들리던 성장 단정을 `verify-codex`에서도 5회 중앙값으로(lategame·p4와 통일 — 초반 고분산은 하네스 문제).
- 전 회귀 **301/301**.


## 2026-07-27 · 거대 천체 스폰 실제 반영 + QA가 라이브 리더보드를 오염시키던 문제(2.7.1)
- **2.7은 반쪽이었다**: 거대 천체 스폰 상한(2.4→4.0배) 치환이 **또 조용히 실패**했다(실제 줄에 주석이 붙어 있어 매칭 실패). 2.4.1과 **완전히 같은 사고**. 이번엔 `assert`로 치환 전후를 검증하고 적용 — 실측 주계열성 최대 2.4배(불변)·블랙홀 최대 4.0배·거대 천체 6.7%.
- **규칙으로 승격**: 문자열 치환은 **반드시 `assert old in s` + 치환 후 `assert new in s`**. 두 번 같은 사고를 냈으므로 예외 없음.
- **[중대] `verify-lb`가 매 실행마다 라이브 리더보드에 실제 행을 썼다** — `QA_PILOT` 33건 + `<b>HAX` 17건 누적(총 50건 삭제, 실제 기록 121건 유지). 게다가 보드가 차면서 XSS 검사 행이 밀려나 **오탐**까지 났다.
  - 수정: `rowHTML` QA 훅을 노출해 **제출 없이 렌더 이스케이프만** 검사. 유효 제출 1건은 엔드포인트 검증용으로 남김(추후 QA 전용 보드 분리 검토 — 코드에 TODO).
  - 교훈: **테스트가 프로덕션 데이터에 쓰면, 그 데이터가 다시 테스트를 깨뜨린다.**
- 밸런스 단정 2건 갱신: `threat still rises with size` → **`late game stays dangerous`**(거대 천체가 늘어 라이벌 '비율'은 내려갔는데 이건 오너가 요청한 방향 — 절대 위험도로 판정 기준 교체). `verify-p4`의 성장 단정도 단일 봇 런이라 흔들려 **5회 중앙값**으로(lategame과 동일 처리).
- 전 회귀 **300/300**.


## 2026-07-27 · 제트를 게임 루프에 편입 + 거대 천체 폭발 연출(2.7)
- 오너: "퀘이사로 적을 흡수해도 속도·크기 변화가 없고 콤보 취급도 안 된다 / 어느 지점부터 블랙홀만 남발해 지겹다 / 거대 행성은 흡수에 시간이 걸리거나 폭발해서 화면이 흔들리면 박진감이 있겠다".
- **① 제트가 게임 루프 바깥에 있었다(확인)**: 콤보·가속·에너지·흔들림 어디에도 안 걸리고 효율만 절반(0.5×). → 콤보 카운트 참여 · 가속 +13% · 에너지 +5% · 흔들림 · `gainPop` 합산 · 효율 0.75×. 실측 제트 8회 포식으로 콤보 1→6, 가속 48%.
- **② 제트와 크기 제한**: 행성·항성은 **크기 무관** 삼킨다(상대론적 제트 앞에서 크기는 무의미). 단 **다른 블랙홀은 제트로 못 먹는다**(실제로도 제트는 사건지평선을 어쩌지 못함) — 대신 **밀어낸다**(수세에서 쓰는 카드). 오너 요청("레벨 없이")을 물리를 깨지 않고 반영.
- **③ "블랙홀만 남발"은 스폰 문제가 아니었다**: 실측 라이벌 19% · 행성 40% · 항성 24%로 원래 다양하다. 진짜 원인은 **거대 천체를 먹을 이유가 없던 것**(2.6에서 이미 완화). 여기에 블랙홀 단계 스폰 상한을 2.4배 → **4.0배**로 올려 진짜 거대 천체가 등장하게 함 — 흡수 2.7초짜리 '사건'이 된다.
- **④ 거대 천체 완식 연출**: 삼킨 것이 자기 질량의 55% 이상이면 **화면 흔들림 + 화이트 플래시 + 굉음 + 26입자 폭발 + DEVOURED 팝업**. 실측 1.9배 천체 흡수 2.74초·플래시 확인.
- **부수 · 안티치트 한계 상향(라이브 반영)**: 위 변경으로 정상 플레이 점수율이 36.5k/s가 되어 서버 한계 40k/s와 여유가 9%뿐 → **정상 기록이 거부될 위험**. 엣지 함수 `accretion-submit` v4로 한계 **40k → 120k/s**. 실측 60k/s 승인·900k/s 거부 확인(치팅 런은 1.3M/s대라 여전히 10배 여유). **한계 실측 중 테스트 행이 라이브 보드에 들어가 즉시 삭제함 — 라이브 DB에 쓰는 검증은 정리까지 한 세트로 할 것.**
- 제트 점수만 콤보 배수 제외(자동 포식이라 배수 보상은 과함) → 점수율 43.5k→36.5k.
- 전 회귀 **299/299**. 밸런스 단정 2건은 새 균형에 맞춰 갱신(병합 우위 3배→2배 — 먹이가 좋아진 만큼 의도된 축소).


## 2026-07-27 · 오너 체감 리포트 4건 수리(2.6) — 전부 실측으로 확인됨
- 오너: "중간에 속도가 너무 느려진다 / 퀘이사가 적을 빨아들일 때 소리가 없다 / 아무 소리도 안 나서 성장하는지 모르겠다 / 성장 게이지가 더 커도 좋겠다 / 어느 순간부터 블랙홀을 안 먹으면 성장이 안 되는 느낌".
- **① 후반 성장 정체 — 리포트가 정확했다.** 실측: 일반 포식 이득이 질량 400 8.6% → 1500 **2.4%** → 6000 **0.62%** → 25000 **0.15%**. 병합 대비 15~18배 열세. `accEffOf` 분모 **120 → 500** 완화(에딩턴 한계 자체는 실제 현상이라 유지). 결과: 6000에서 0.62%→**2.4%**, 병합은 여전히 3.8배 우위. 후반 경로는 병합이지만 **일반 포식도 체감된다**.
- **② 속도 — 체감 속도는 원래 일정했다(0.80).** 월드속도/카메라거리 비가 전 질량대에서 0.801~0.808. 즉 "느려진다"의 진짜 원인은 2.3에서 넣은 **흡수 중 감속(BH 0.6×)**. 0.78×로 완화하고, 오너 제안대로 **포식 가속** 신설 — 먹을 때마다 +17%(흡수 완료 +24%), 최대 +60%까지 누적, 0.62/s로 감쇠. 실측 +14% 즉시 상승 후 1.2초에 소멸. 근거: 낙하 물질이 각운동량을 넘긴다.
- **③ 소리 — 제트는 실제로 무음이었다.** WebAudio 노드 생성을 계측해 확인: 제트 포식은 `AU` 호출이 아예 없었다(osc 0). `AU.jet()` 신설 + 큰 천체 흡수 완료에 `AU.swallow()` 신설 + 포식음 볼륨 .055→.085. 계측 결과 포식 osc+39 · 삼킴 osc+8 · 제트 osc+21.
- **④ 성장 게이지 확대**: 폭 340→470px(70vw), 트랙 높이 7→12px, 라벨 9→10.5px.
- 전 회귀 **299/299**. 옛 밸런스를 고정하던 단정 3건은 **의도를 유지한 채** 갱신(일반 포식 "거의 안 큰다" → "체감되되 병합이 3배 이상 낫다", 감쇠 임계 0.05→0.16). `threat still rises` 는 20 vs 5000 차이가 몇 %p라 12표본으로 동률이 나던 흔들림 → 45표본으로 확대.


## 2026-07-27 · 세계관 정정 — 미행성 → 원시별, 붕괴 임계값 20 M☉(2.5)
- 오너 승인: 계획서 권장안(전체 진행 + 임계값 A=20 M☉). "실제 천체물리에 위반하지 않게, 재밌게, 다각도로, 검증까지".
- **왜**: 게임이 "실제 천체물리를 게임 규칙으로"를 내걸었는데 **첫 고리만 사실이 아니었다** — 별은 암석을 모아 생기지 않고 **가스 구름의 붕괴**로 태어난다. 주계열성 이후는 원래 교과서와 일치했다.
- **변경 5곳**: `FORMS[0].n` PLANETESIMAL→**PROTOSTAR** · `drain` ERODING→**OUTFLOWS**(원시별의 실제 질량 손실=쌍극류) · 1단계 메시 회색 암석→**수축열로 붉게 달아오른 구름 핵** · `FORMS[1].max` **12→20**(약 20 태양질량 이상만 붕괴해 블랙홀이 된다) · 코덱스 **23항목**(PROTOSTAR 추가) + 첫 판 배너 "a collapsing cloud core — not yet a star".
- **페이싱 검증(계획서 3단계)**: 동일 하네스 40런 A/B — 블랙홀 도달 중앙 **1.20s → 1.50s(1.25×)**, 주계열성 구간 0.70s → 1.00s(1.43×). 사전 게이트가 "도달 1.35× 초과 시 보정"이었고 **1.25×로 통과** → 보정 없이 채택. 덧붙여 주계열성이 길어지는 것은 실제로도 맞다(태양 수명 100억 년 중 90억 년이 주계열성).
- **부수 발견 · 기존 버그 2건 수리**:
  ① **초신성 충격파가 한 프레임도 보이지 않았다** — `supernova()`가 `shockT=1`로 시작하는데 `opacity=0.9*(1-shockT)`=0이고 `shockT>=1`이면 즉시 숨김. `shockT=0`으로 수정하니 링이 실제로 퍼진다(스크린샷 확인). 게임 출시 이후 계속 죽어 있던 연출.
  ② **원시별에서 블랙홀 질량으로 한 번에 뛰면 초신성이 안 터졌다**(`nf===2&&wasStar` 가드). 별 단계를 건너뛰고 항성질량 블랙홀이 되는 경로는 실제로 없으므로 `prev<2`면 무조건 붕괴하도록 수정.
- **테스트**: 신규 `verify-lore.mjs` **15/15**(19.4 M☉는 블랙홀 아님 · 13 M☉도 아님(옛 임계값 회귀 방지) · 20.6에서 붕괴 1회 · 오르내려도 재폭발 없음 · PROTOSTAR 코덱스 개방 · 사슬 일치 · 드레인 문구). 기존 단정 갱신 8파일. 전 회귀 **299/299**.
- **테스트 교훈**: `shockOn()`(링 표시 여부)으로 "초신성 횟수"를 재던 두 단정이 **프록시라서 틀렸다** — 링이 원래 안 보였기 때문에 0회로도 통과했고, 링을 고치자 지속 시간 때문에 오탐이 났다. `novaCount()` 훅을 추가해 **사건을 직접 세도록** 교체.
- undefined-studio 등재 설명도 원시별·20 M☉ 기준으로 동기.


## 2026-07-27 · 화면 문구 겹침 4건 수리(2.4.3) — 2건은 기존 결함
- 오너: "콤보 표시 문구들이 겹쳐 보인다".
- **감사 방법**: 선택자를 추측하지 않고 `position:fixed` 요소를 전부 열거해 **텍스트 리프 단위**로 쌍별 겹침을 실측(`/tmp/ovcheck.mjs`, 3뷰포트 × 실제 프렌지 상태). 처음엔 선택자를 손으로 나열했다가 **APEX 칩을 놓쳤다** — 열거 방식으로 바꾸고 나서야 잡혔다.
- **적발 4건**:
  ① `#combo`(top 38%) ⨯ `#banner`(top 34%) — 둘 다 중앙 정렬이라 **40px 포개짐**(오너가 본 것).
  ② **코덱스 토스트 문구 ⨯ DASH/PULSE 칩** — 토스트 `bottom:26px` 위에 `#surge`가 z-index 7로 얹힘(**기존 결함**).
  ③ **팝업 ⨯ 팝업** — 연속 포식 시 **동시 72개·겹친 쌍 923개**가 같은 지점에 쌓임(진짜 주범).
  ④ 모바일 가로에서 팝업이 배너 띠까지 침범.
- **수정**: 콤보 → 점수 열 아래(우상단, 중앙 띠에서 이탈) · 코덱스 토스트 `bottom:78px`(컨트롤 위) · 배너 폭 `min(560px,62vw)` 제한 + 상단 하한 `max(23%,128px)` · 팝업 9칸 고정 슬롯 + 배너/하단 UI 침범 클램프 · **연속 포식 이득을 0.42s 창에서 하나로 합산**(`gainPop`).
- 합산이 핵심: "+40 ×58" 9개 대신 "+175 ×72" 하나 → 겹침이 사라지고 **정보량은 오히려 늘었다**.
- 결과: 1280·1440·844 **세 뷰포트 모두 겹침 0**(감사 전 5·4·10건). 전 회귀 281/281.
- 교훈: **오버레이 감사는 선택자를 손으로 나열하지 말고 DOM에서 열거할 것.** 손으로 적으면 반드시 빠뜨린다.


## 2026-07-27 · 아크 마스크 실제 제거(2.4.2) — 2.4.1은 반쪽 적용이었다
- 2.4.1에서 "연결형 아크"를 적용했다고 보고했으나, **치환 문자열이 실제 파일과 달라 조용히 넘어갔다**(python `str.replace`는 미스매치 시 예외 없이 원본 유지). 밝기(uOp 0.52→0.33)만 적용돼 라이브에는 여전히 마스크된(끊긴) 아크가 나갔다.
- **적발 경로**: 배포 후 라이브 파일을 grep으로 검사하는 습관 — `smoothstep(0.30,0.82` 가 0건이어야 하는데 1건. 로컬 렌더만 봤으면 못 잡았다.
- 수정: 마스크 라인을 **줄 단위 필터로 제거**하고 개수를 assert. 이제 미적용이면 스크립트가 죽는다. 쓰이지 않게 된 `uArc` 유니폼도 선언·전달부에서 정리.
- 교훈: **문자열 치환은 성공을 보장하지 않는다 — 치환 후 결과를 세어 검증하거나 assert를 걸 것.**
- 전 회귀 281/281(verify-menu는 배치 중 1회 흔들렸으나 단독 3회 재실행 모두 17/17).


## 2026-07-27 · 오너 피드백 반영 — 렌즈 아크 연결 + 하단 UI V4(2.4.1)
- 오너: "원반 위에 뭐가 있는데 없는 게 낫지 않나" / "아래 UI가 텍스트 위주라 무엇이 시작 버튼이고 무엇이 정보인지 안 보인다" → 시안 공유 후 **연결형 아크 + V4** 승인.
- **아크**: 원인은 원반 평면 근처를 지운 마스크(`smoothstep(|sin a|)`) 때문에 **아크 양끝이 원반과 끊긴 것**. 마스크를 걷으면 아크 좌우 끝이 원반 좌우 끝과 기하학적으로 **같은 점**이라 하나의 고리가 된다. 밝기 0.52→0.33으로 낮춰 원반이 주인공으로 남게.
- **하단 UI = V4(3단 위계)**: 진짜 문제는 스타일이 아니라 **정보 구조** — BEST·RANK·CODEX·AWARDS가 전부 눌리는 버튼인데 통계처럼 생겼었다. ① **행동**=화면에서 유일하게 형태를 가진 요소(ember 박스 BEGIN) ② **보조 행동**=밑줄 링크(CODEX/LEADERBOARD/AWARDS) ③ **기록**=맨 아래 가장 흐린 활자(BEST/RANK). PILOT은 좌하단 구석으로.
- 핸들러 보존: `startDailyBoard`를 RANK 통계에 물려 리스너를 유지(요소를 지우면 부팅 시 null 참조).
- 부수 수리: V4가 행을 하나 더 써서 **워드마크가 위로 밀려 블랙홀과 겹침** → 히어로 카메라 shift 0.20→0.34로 구체를 올리고 블록을 내림. 3개 뷰포트 실측 확인.
- 전 회귀 **281/281**.


## 2026-07-27 · 메인 화면 TITLE CARD 전환 + 블랙홀 셰이더 재작(2.4)
- 오너: "메인화면 폰트·UI가 유치하다, 더 트렌디하게" → 시안 3안 제시(관측기록/타이틀카드/계기판) → **B 타이틀 카드 채택**. 이어 "블랙홀 디테일이 떨어진다".
- **진단(유치함의 실체 6가지)**: UI 전면 모노스페이스(15선언)·워드마크 그라데이션+글로우(템플릿 표식)·알약형 그라데 CTA(SaaS 버튼)·동일 무게 알약 칩 4개(위계 없음)·남색+노랑(완구 배색)·중앙정렬 6블록 수직 나열(구성 아님).
- **적용**: 레터박스 + **Anton** 워드마크(자간 .34em) + **Martian Mono** 크롬. 알약·글로우·그라데이션 전부 제거, 기록은 테두리 없는 활자 레일로. 폰트는 **base64 임베드(77KB)** — 단일 파일·오프라인 유지.
- **블랙홀 셰이더 재작**: 얇은 토러스(평평한 고리) → **RingGeometry + 커스텀 GLSL**. 실제 블랙홀 이미지의 3대 특징을 넣음 — ① **케플러 전단**(안쪽이 r^-1.5로 빠르게 감김) + fbm 난류 ② **온도 구배**(안쪽 뜨거움 → 바깥 식음, 스킨 색으로 유니폼화) ③ **상대론적 도플러 빔**(접근측 밝기 pow(1+βcos,3)·청색이동). 추가로 **렌즈 아크**(그림자 위아래로 휘어 넘어오는 뒤쪽 원반)와 또렷한 **광자 고리**(depthTest 끔·renderOrder 6).
- **핵심 버그 2건(측정으로 확정 — 메시를 하나씩 꺼서 격리)**: ① `diskArc.rotation.z`는 **ring 자기 평면 안 회전이라 기하학적으로 무효** → 아크가 원반과 같은 평면에 누워 있었다. XY 평면 배치로 수정. ② **사건지평선(0.98)이 원반 안쪽 반경(1.0)과 거의 같아** 얕은 각도에서 원반 앞쪽이 그림자 아래 절반을 덮어 '돔' 실루엣이 됨 → 실제 비율(그림자 ≪ ISCO)대로 **0.62 / 원반 안쪽 0.86**으로 조정.
- 부수: 가산 합성에서 R이 먼저 포화돼 색이 노랗게 뜨는 문제 → 밝기를 전부 alpha로 보내 **색상 보존**. 별·먼지 파티클이 **네모 블록**으로 보이던 것 → 원형 스프라이트 텍스처.
- 스킨 채색이 정점색 기반이라 셰이더 전환 시 `paintDisk`가 깨짐(`diskGeo` 미정의) → **유니폼(uCA/uCR)으로 이전**, 3스킨 전부 원반·아크·광자 고리에 반영 확인.
- 전 회귀 **281/281**.
- **미결(오너 결정 대기)**: 세계관 1단계 개명(미행성 → 분자운 핵/원시별)과 초신성 임계값 12 → 20 M☉. 별이 암석을 모아 생기지 않는다는 실제와의 불일치 — 게임플레이 변경 없이 이름·먹이 라벨만 바뀜.


## 2026-07-27 · 오너 아이디어 2종 — 조석 탈출 · 점진 흡수(2.3)
- 오너: "① 큰 블랙홀에 부딪히면 한 번에 끝나는데, **사건지평선 안으로 들어가면 세력을 뺏기되 벗어날 기회**를 주면? 다 뺏기면 죽고. ② 모든 행성이 한 번에 먹히는데, **행성일 땐 다른 행성 먹는 데도 약간의 시간**이, 블랙홀이 되면 작은 건 한 입에 · **자기보다 큰 건 시간이 걸리게**."
- **(A) TIDAL ZONE**: 큰 라이벌의 입 반경(1.55R) = 즉사 → **조석 영역**으로 전환. 안에 있으면 질량을 초당 22~107%(깊이 비례) 뜯기고 안쪽으로 끌려든다. 벗어나면 생존 + **TIDAL ESCAPE 보너스(질량×2)**, 다 뜯기면 소멸. **0.72R 안쪽(진짜 사건지평선)은 여전히 즉사** — 넘으면 되돌릴 수 없다는 실제 물리. 조석 영역 안에선 GRAZE 보너스가 안 나오게 `else if`로 분리(뜯기는 중에 보상 금지).
- **(B) 점진 흡수(TDE)**: 상대 크기 기준 — 블랙홀은 **rel≥0.6**(자기보다 큰 쪽), 행성·항성 단계는 **rel≥0.5**부터 흡수에 시간. 지속 = BH `0.4+1.3·rel`(≈1.4~2.7s) · 초반 `0.2+0.5·rel`(≈0.45~0.8s, "약간의 시간"). 흡수 중엔 **질량이 점진 이전**되고 대상이 눈에 띄게 줄며, 이동이 느려지고(BH 0.6× · 초반 0.85×) **감소(증발/침식)는 멈춘다**(실제로 물질이 유입 중). 하단에 **DEVOURING 진행 바**.
- **병합은 즉시로 유지** — 실제 블랙홀 병합은 밀리초(중력파 링다운)이고, 시간을 걸었더니 후반 성장이 **순감 −10.4%** 로 뒤집혔다(흡수 시간 동안 증발이 계속 돌아서). 측정으로 잡아 되돌림.
- 신규 코덱스 **TIDAL DISRUPTION**(22항목) — "별을 삼키는 건 느리다: 조석파괴사건은 수개월~수년간 블랙홀을 먹인다."
- **[별건 · 기존 결함 2건 동시 수리]** ① 데일리 **HUNTED**가 블랙홀 형태 게이트에 막혀 "처음부터 쫓김"이 작동 안 함 → 형태 게이트 우회. ② 암석 단계 흡입권이 좁아 오프셋 5 near-miss를 놓침 → rock `pull` 2.2→3.4(**pull은 먹을 수 있는 천체에만 적용** → 위험 천체가 끌려오지 않음. 초반 급사 분산도 완화).
- 검증 신규 tidal **20/20** + 전 회귀 **281/281**.
- **하네스 결함 2건 수리(측정으로 판정)**: `verify-lategame`의 성장 단정은 단일 봇 런이라 원래 ~5% 흔들림 — 40회 표본 비교 결과 **변경본이 기준선보다 오히려 나음**(손실 1/40 vs 2/40 · 중앙 질량 2.94 vs 2.68 · 사망 8 vs 7) → 5회 중앙값으로 교체. `verify-ecology`의 "라이벌 가식률 ≥22%"는 실측 **32%±5**인데 10라운드 표본(n≈96) 꼬리가 21%까지 내려가 스침 → 표본 40라운드로 확대. **둘 다 게임 결함이 아니라 테스트 결함이었다.**


## 2026-07-27 · 오너 버그 — 퀘이사 제트 "머리에 빔 하나 · 안 먹었는데 빨려듦"(2.2.1)
- 오너: "블랙홀 머리에 빔 같은 게 뭔가? 생긴 뒤로 **행성을 먹지 않았는데 가만히 있어도 빨려 들어온다. 실제로 먹히는지도 모르겠다.**"
- **버그 3건 확정**:
  ① 두 제트가 **똑같이 위(+Y)를 향함** — `jg.translate(0,sy*4.7,0)` 뒤의 `jm.rotation.x=Math.PI`가 이미 이동된 지오메트리를 되뒤집어, 아래쪽 제트가 위로 접혔다. → "머리에 빔 하나".
  ② **보이는 축(Y) ≠ 판정 축(XZ 평면을 도는 선)** — 탑다운 뷰에서 메시는 카메라 쪽(Y)을 향하는데, 소비 판정은 XZ 평면을 회전하는 직선이었다. 즉 **보이지 않는 회전 칼날**이 주변 천체를 삼킴 → "안 먹었는데 빨려든다".
  ③ 판정 폭 `hr*1.5`가 어떤 시각 요소보다 넓어 더 멀리서도 사라짐.
- **수정**: 제트를 **플레이 평면(±X)에 눕히고** 회전 — 지오메트리에 회전을 구워 `CylinderGeometry(끝 0.62/뿌리 0.14, 길이 9)`로 collimated 빔 2개. 판정 범위를 **보이는 빔과 동일**(길이 9·폭 tip×1.15)하게 맞추고, 소비 시 **빔이 번쩍**여(opacity 0.5→0.92) "이 빔이 먹었다"가 보이게. 회전 0.9→0.75로 완만.
- 검증 신규 jets **13/13**: 제트 2개·월드 팁이 **정반대**(+10.6/−10.6)·평면에 누움·**축 위 천체는 먹히고 축에서 90° 벗어난 천체는 안 먹힘**(각도 0·1.1rad 양쪽)·빔 길이 밖 미소비·QUASAR 미만 제트 없음+자동소비 없음·질량/점수 실제 증가. + 전 회귀 **235/235**.
- 테스트 교훈: 회전을 **지오메트리에 구우면** 메시 quaternion으로 방향을 못 잰다 → 월드 위치로 판정. `verify-midgame`은 5티어 확장으로 mass 20/400이 가까워져 노이즈 → 20/5000으로 간격 확대(4연속 통과).


## 2026-07-26 · 오너 지적 — "블랙홀에선 사이즈만 커지고 정체"(2.2, 5단계 진화)
- 오너: "더 여러 단계로 진화 못하나? 블랙홀에선 사이즈만 조금씩 커지고 정체하는 것 같다."
- **원인 확정**: 블랙홀 티어가 `45/160/1e9` 3단계뿐 → **160을 넘으면 끝**. 오너가 도달한 36,700까지 **200배 성장하는 동안 아무 변화가 없었음.**
- **5단계로 확장**(로그 간격 ×10, 실제 천체 분류 기준):
  `STELLAR-MASS(120) → INTERMEDIATE(1,200) → SUPERMASSIVE(12,000) → **QUASAR**(120,000) → **ULTRAMASSIVE**` — 후반에도 계속 승급한다.
- **단계마다 실제로 달라진다**: 존 팔레트 5종(청·보라·금·**백청 화염**·**심홍**), 렌징 1.0→**2.1** 연속 상승, 티어업 배너에 단계 설명.
- **QUASAR 단계 = 상대론적 제트 점화**(실제 퀘이사의 특징): 극에서 뻗은 두 빔이 회전하며 **축선상의 천체를 삼킨다**(절반 효율). 블랙홀 구간에 처음으로 "형태가 바뀌는" 사건이 생김.
- 코덱스 21항목(**ULTRAMASSIVE** — TON 618은 태양 660억 개). 티어업 점수도 절대값(500·티어)에서 **질량 비례**로.
- 검증 신규 tiers **11/11**(5티어·질량 사다리가 각기 다른 티어·존 색 5종·렌징 상승·제트 OFF/ON 경계·제트 포식 실측·오너 peak 36.7K가 이제 QUASAR) + 전 회귀 **222/222**.


## 2026-07-26 · 오너 지적 — "블랙홀이 너무 많다·성장이 멈춘다"(2.1, 후반 루프 재설계)
- 오너 런 실측: SCORE 277,864,138 · PEAK 36.7K · 218초 · **score not ranked** · 화면 전체가 거대 라이벌로 덮인 스샷.
- **실측 진단 3종**: ① 후반 한 입 증가 **+0.095%** = 사실상 정지 ② 라이벌 스폰 0.7~2.8× → **먹을 수 있는 건 14%**뿐이고 나머지는 반경 1.41×의 벽 ③ 점수가 **절대 질량** 기준이라 218초에 233M → **상한(1e8) 초과로 등록 거부**.
- **핵심 수정 — 후반 성장 경로를 "블랙홀 병합"으로**(오너 모델과 일치, 실제로도 초대질량 블랙홀은 병합으로 자란다):
  - 라이벌 포식 = **병합**으로 취급, 강착과 **다른 감쇠 곡선**(`mergeEffOf` = /2000, 강착은 /120). 병합은 복사압 제약이 약하다는 물리와 일치.
  - 실측: 질량 20,000에서 일반 먹이 **+0.175%** vs 병합 **+3.29%**(19배), 200,000에서 20배 — **후반엔 블랙홀을 사냥해야 큰다**가 실제로 성립. 첫 병합 시 안내 배너.
  - 감쇠 덕에 여전히 **선형**(220초 이상적 포식 = 질량 68,570, 이전 5.6억에서 폭주 차단).
- **라이벌 압축**: 스폰 0.7~2.8× → **0.55~1.86×**(먹을 수 있는 비율 14%→**37%**, 최대 반경 1.41→1.23×), 밀도 상한 0.22→**0.18**.
- **점수 재설계**: `절대 질량×8` → **`실제 흡수 질량×13`**. 초반 점수는 그대로(계수상 거의 동일)이고 후반 폭주만 제거 → 220초 런이 344만으로 **등록 가능**.
- **백엔드**: 점수 체계가 바뀌어 안티치트 레이트 상한 12k/s → **40k/s**(정상 장기런 15.7k/s 통과·깨진 런 1.27M/s는 여전히 거부 — curl 실검증).
- 검증 신규 4종 포함 ecology **22/22** + 전 회귀 **211/211**. 패치 중 주석 꼬리가 코드로 남아 부팅이 깨진 건(`missing ) after argument list`) 즉시 수리.


## 2026-07-26 · 오너 지적 — "적들이 더 빨리 큰다 · 끝엔 갇혀서 끝난다"(2.0)
- 오너 런 실측(스샷): SCORE 44,296,772 · PEAK **45,668 M☉** · 155초 · 랭크 #1 · DEVOURED. "마지막에 거의 **갇혀서** 끝나는 것 같은데 적절한가? 뭔가 **적들은 레벨업을 더 빨리** 하는 느낌".
- **직감이 정확했음 — 원인은 에딩턴 한계를 플레이어에게만 적용한 것**. 라이벌 포식은 `bq.mass*0.4`로 **감쇠 없이** 먹고 있었다. 한 입 증가 비교: 질량 200에서 1배 → 2,000에서 **11배** → **45,668(오너 런)에서 246배**. 라이벌이 순식간에 상한(내 질량 3.4×)에 붙어 **주변이 전부 못 이기는 거인**이 되고, 그래서 "갇혀서 끝나는" 엔딩이 나옴. **두 불만이 같은 원인.**
- **수정**: 에딩턴 한계를 `accEffOf(m)`으로 일반화해 **라이벌에도 동일 적용**(에딩턴은 블랙홀의 성질이지 플레이어의 성질이 아니다). 상한도 3.4×→**2.6×**. 결과 격차 **246배 → 0.65배**(이제 플레이어가 더 빠름).
- **부수 버그**: 상한을 `Math.min(total, cap)`으로 걸어 **이미 상한을 넘은 라이벌의 질량을 깎던 것**(300→260) → `if(a.mass<cap)`로 변경. 먹어서 줄어들 수는 없다.
- 검증 신규(성장 대칭성) 포함 ecology **16/16** + 전 회귀 **204/204**.


## 2026-07-26 · 오너 버그 — "먹지도 않은 행성이 계속 먹어진다"(1.9.1)
- 오너 리포트: 어느 시점부터 **내가 먹지 않은 행성들이 계속 빨려 들어온다**.
- **원인**: 먹힘 애니메이션이 `o.g.position.lerp(hole.position,…)` — **누가 먹었든 무조건 플레이어를 향해** 빨려들도록 하드코딩돼 있었음. 1.7에서 **라이벌도 객체를 삼키게**(`bq.eaten=true`) 하면서, 먼 곳의 라이벌이 먹은 행성들이 전부 **플레이어에게 날아오는** 현상이 생김. 시각적으로는 "안 먹었는데 먹힌다"로 보임.
- **수정**: 먹은 주체를 `eatBy`로 기록하고 **그 주체 쪽으로** 빨려들게(기본=플레이어, 주체가 사라졌으면 플레이어로 폴백). 초신성 일소는 플레이어 소유로 명시(`eatBy=null`).
- 검증: 라이벌 옆의 특정 돌을 추적 — 먹힌 뒤 플레이어까지 거리 **71.5 → 69.7**(라이벌 곁에 머묾, 이전엔 0으로 수렴). 전 회귀 **202/202**.
- 테스트 하네스 교훈: `eatenInfo` 전체의 최소거리를 비교하면 **매 샘플이 다른 객체**라 무의미 → 특정 객체 추적으로 교체. `60초 실플레이 peak` 지표는 약한 봇의 생존 편차가 커(504→166→31) **결정론적 성장 측정**으로 대체.


## 2026-07-26 · STEP 4 — 루프 깊이(능력 2종·그레이즈 연쇄) + 온보딩(1.9) · 권장안 4단계 완료
- **능력 2종**: 기존 `⚡ DASH`(속도 2.3×·포식반경 1.6× = 사냥) 옆에 **`◉ COLLAPSE PULSE`** 신설 — 반경 내 **먹이는 입 앞으로 끌어당기고 위협은 밀어낸다** + 0.6s 무적. 같은 에너지를 두고 **"사냥이냐 수확·탈출이냐"** 선택이 생김. PC `Space`/`Shift`, 모바일은 게이지의 두 버튼.
- **그레이즈 연쇄**: 3.2초 내 연속 그레이즈 시 배수 ×1~×6 — 충전량·점수 모두 배수 적용, 팝업 `GRAZE ×N`. **위험한 플레이가 곧 강해지는 플레이**라는 축을 심화(nova-surge 검증 훅).
- **온보딩**: 첫 런에만 4단계 힌트(이동 → 초록을 먹어라 → 빨강·주황 회피 → 성장하면 진화). 검증 중 **신규 유저는 증발로 11초쯤 죽는다**는 사실이 드러나 힌트 시점을 15초 → **9.6초**로 앞당김.
- **버그 수리**: 온보딩 플래그가 `const`(로드 시 고정)라 **같은 세션 두 번째 런에서 힌트가 반복**되던 것 → `let`+런타임 갱신.
- 검증 신규 depth **13/13**(HUD 2능력·펄스 소모/비대시/카운트·먹이 견인 30→6·미충전 게이팅·그레이즈 연쇄 ×4·점수·비살상·첫런 4힌트·재실행 미반복) + **전 회귀 200/200**.
- **권장안 4단계 완료**: STEP1 진화 아크 · STEP2 우주 현상 · STEP3 업적+주간보드 · STEP4 루프 깊이+온보딩.


## 2026-07-26 · STEP 3 — 업적 20종 + 주간 보드(1.8)
- **업적(AWARDS) 20종** = 코덱스 옆의 **두 번째 수집축**. 플레이 방식에 이름을 붙여준다: FIRST LIGHT(점화)·COLLAPSE(초신성 생존)·APEX HUNTER/TRIPLE·LONG BURN(90초)·CENTURION/TITAN/GALACTIC CORE(질량)·BRUSH WITH DEATH(그레이즈 20)·FEEDING FRENZY(콤보 30)·SPELUNKER(웜홀 3)·SLINGSHOT(서지 10)·SCAVENGER(잔해 3)·STAR NURSERY(성운)·UNBROKEN(무충돌 블랙홀 도달)·STARGAZER/ARCHIVIST(코덱스)·DAILY RUNNER·MILLIONAIRE·COLLECTOR(스킨 전부).
  - 런 카운터 6종 신설(그레이즈·웜홀·서지·잔해·성운·충돌) → `checkAch()`가 매 프레임+게임오버에 평가. localStorage `acc_ach` 영구 저장, 획득 시 배너.
  - **화면**: 코덱스 오버레이에 **탭(◆ CODEX / ★ AWARDS)** 추가 — 제목·설명·그리드·스킨영역이 탭에 따라 전환. 시작화면에 **AWARDS 칩** 추가.
- **주간 보드**: 보드 오버레이를 **3모드(ALL TIME / THIS WEEK / DAILY)** 세그먼트로. 주간은 `created_at >= 월요일(UTC)` 필터 — **스키마·엣지함수 변경 없이** PostgREST 필터만으로 구현. `weekStart()`가 ISO 주(월요일) 계산.
- 검증 신규 meta **21/21**(업적 리셋·마일스톤 4종 해금·카운터 기반 해금·리로드 영속·탭 2개·그리드 20개·획득 표시·스킨영역 복구·주 시작이 월요일·보드 3모드·주간 제목·신규 런이 주간 보드에 노출) + 전 회귀 **187/187**.
- 테스트 하네스 교훈 2건: ① 서지는 지속 0.9초 중 재발동 불가 → 그만큼 진행 필요 ② 가만히 있으면 라이벌에게 죽어 후속 동작이 막힘 → 필드 비우고 측정.


## 2026-07-26 · 오너 설계 피드백 3건 — 크기 위계·객체 상호작용·후반 성장(1.7)
- 오너: ① "어느 순간부터 블랙홀 키우기가 너무 힘들다(거대 블랙홀이 많아져 레벨업 어려움)" ② "**적 행성들도 서로 부딪혔을 때 상호작용해야** 하지 않나" ③ "**내가 블랙홀이 아닐 땐 나보다 상위 행성과 부딪히면 부서져야** — 지금은 블랙홀 아닌 건 다 먹힌다".
- **③이 구조의 빈 곳을 정확히 지적**: 스폰이 비-라이벌을 항상 내 질량보다 작게 만들어서, **행성·항성 단계엔 위협이 아예 없었음**(라이벌 블랙홀만). → **크기 위계 재도입**: 14% 버킷을 **나보다 큰 천체**(1.15~2.4×)로 스폰. 행성/항성 단계에선 **충돌 = 상호 파괴**(내 질량 −22%·상대 −45%·서로 튕김·콤보 리셋, 즉사 아님). 후광 3등급: **초록 먹이 / 주황 충돌위험 / 빨강 즉사**.
- **블랙홀은 질량 무관 전부 삼킨다**(물리적 사실) → 같은 큰 천체가 블랙홀이 되면 **큰 식사**로 바뀜 = 진화의 보상이 명확해지고 ①의 성장 문제도 완화.
- **② 객체 간 상호작용**: 라이벌 블랙홀이 주변 천체·서로를 삼킨다(성장 상한 = 내 질량 3.4×). 필드가 **스스로 정리**돼 "거대 블랙홀만 누적"이 완화되고, 남의 포식을 이용할 수 있음. 실측 라이벌 300→340.
- 코덱스 20항목(collision — 미행성 충돌이 소행성대를 만들었다).
- 검증 신규 ecology **12/12**(큰 천체 스폰·행성/항성 못 먹음·충돌 상호파괴·즉사 아님·블랙홀은 전부 포식·라이벌 포식·60초 실플레이 peak 504) + 전 회귀 **154/154**. 옛 가정 검사하던 테스트 2건은 새 사양으로 갱신(비-라이벌 100% 먹이 가정 폐기).


## 2026-07-26 · 오너 플레이 피드백 — 위협 식별 불가·후반 성장 정지(1.6)
- 오너: "재미는 있다. 다만 **블랙홀이 되면 나보다 큰 적을 구별하기 어렵고**, **성장이 멈춰** 키우기 어렵다. 의도한 건가?" → **둘 다 의도 아님, 수치로 원인 확정.**
- **① 식별 불가 — 근본 원인은 기하학**: 반경 = 질량^⅓라 **크기 차이가 압축**된다. 나보다 **1.5배 무거운(=즉사) 라이벌이 화면에선 1.14배**, 2.8배여도 1.41배. **눈으로 구분 불가능**하고 유일한 신호인 후광(얇은 링)은 라이벌 자신의 주황 글로우에 묻혔음. → **라이벌의 강착원반·글로우 자체를 판정 색으로 칠함**(위협 `#ff3040` 빨강 / 먹이 `#66e84a` 초록) + 위협 후광 강화(0.55→0.74). 원반이 가장 큰 시각 요소라 즉시 판별.
- **② 성장 정지 — 과교정**: 에딩턴 감쇠 `/45`가 과해 한 입 증가율이 mass 400에서 **3.6%**, 1000에서 **1.4%**, 3000에서 **0.4%** = 사실상 정지(폭주 330억을 막으려다 반대편으로 감). → **`/45` → `/120`**. 한 입 절대 증가가 후반에도 **34 안팎 유지**(이전 13), 여전히 선형(폭주 없음). 실측 90초 이상적 포식 = mass 6,348·score 844만(제출 가능).
- 검증 신규 readability **12/12**(위협 빨강·먹이 초록·색 상이·크기 단서 무력함 문서화·mass 200/1000/3000 성장 지속·90초 범위·점수 제출가능) + 전 회귀 **142/142**. `verify-midgame`은 위협 비율 분산이 커 표본 4→12회로 안정화(3연속 통과).


## 2026-07-26 · 리더보드 리셋(권장안) + STEP 2 우주 현상 3종(1.5)
- **리더보드**: 오너 "권장안대로". ① 서버 `accretion_runs` **전량 삭제**(29건 — 진화 아크·에딩턴 이전 밸런스 기록이라 새 규칙과 비교 불가) ② **점수 버전 스탬프**(`acc_scorever='2'`) 도입 — 버전이 다르면 `acc_best`·`acc_rank`만 자동 초기화(폭주 버그로 남은 2조 기록이 영원히 안 깨지는 문제 제거). **코덱스·스킨·파일럿 이름은 보존**(수집은 밸런스 무관). 출시 직전 최종 1회 리셋 후 영구 보존 예정.
- **STEP 2 — 우주 현상 3종**(맵에 "가볼 이유" 부여, 전부 실제 현상):
  - **NEBULA(성운)** — 반경 26의 가스 구름. 안에 머무는 동안 **질량 회복 + 서지 충전** = 증발과 싸우는 안전지대. 밖에선 여전히 감소(공짜 없음).
  - **WORMHOLE(웜홀)** — 접촉 시 **먼 곳으로 순간이동**(실측 105유닛) + 0.8s 무적. 포위·네메시스 추격의 탈출구. 1회용(사용 시 소멸).
  - **SUPERNOVA REMNANT(잔해)** — 접촉 시 **대량 질량 + 점수**(실측 +15 질량·+210). 에딩턴 효율 적용.
  - 등장 게이트: **항성 단계부터**(미행성 구간은 기본기에 집중), 블랙홀이면 동시 2개. 뷰 밖으로 멀어지면 정리.
- 코덱스 16→**19항목**(nebula/wormhole/remnant), 스킨 임계 8/19.
- 검증 신규 features **16/16**(게이팅·성운 회복 vs 밖 감소·웜홀 순간이동/소모·잔해 보상·코덱스 3종·리셋·회귀) + 전 회귀 **132/132**.


## 2026-07-26 · 메인화면 재설계 v2 — 시안 C(PORTAL) 채택 + 메뉴 렌더 버그 2건(1.4)
- 오너 "메인화면 퀄리티가 별로" → 시안 3종 제시(Artifact: ECLIPSE·OBSERVATORY·PORTAL). **오너 C 선택**, 단 "블랙홀 표현이 이상하다 — 시공간이 휘는 느낌이 있어야".
- **그 지적이 실제 버그를 드러냄**(메뉴 실측): ① `applyForm`이 초기에 호출되지 않아 **암석·항성·블랙홀 메시가 전부 동시 렌더**(`rock:true star:true bh:true`) → 겹쳐 보여 "이상한 표현". ② 메뉴에선 플레이어가 미행성 형태라 **렌징 배수 0.14 → 실제 강도 0.0035** = 게임의 시그니처(시공간 왜곡)가 사실상 꺼져 있었음.
- **수정**: 메뉴 히어로를 **블랙홀 형태로 고정**(`applyFormInit`), 메뉴 전용 렌징 **0.0035 → 0.082**(배경별이 아크로 휨) + 광자링을 얇고 또렷하게(uEH 0.105→0.058, 뿌연 후광 제거) + 히어로 스케일 2.3배(사건지평선 선명).
- **레이아웃 C**: 카메라 오프셋을 x(우측) → **z(위쪽)** 로 바꿔 홀을 화면 상단(y≈0.34)에 두고, 아래로 워드마크(system-ui 900·타이트 자간·그라디언트) → 태그라인 → PILOT → **거대 PLAY** → 조작키 → **클릭 가능한 칩 3종**(BEST/RANK/CODEX). 우상단 **데일리 리본**. 기존 스탯 스트립·링크 줄 제거(옹색함 해소).
- 검증 menu **17/17**(칩·리본·히어로 위치·HUD 게이팅·좁은화면/모바일 적합) + 전 회귀 **116/116**.


## 2026-07-26 · 오너 밸런스 피드백 — 후반 포위감·다음 단계 진입 난이도(1.3)
- 오너: "거대 블랙홀이 되면 **적 블랙홀이 너무 많아지고**, 타이밍을 놓치면 다음 단계로 가기 어렵다".
- **실측 진단**: 치명 위협 비율이 mass 20→50에서 **10%→19%로 두 배 점프**(티어 경계 절벽). 같은 지점(45)에서 **네메시스까지 등장**, 곧이어 에딩턴 감쇠(60~)가 시작 → **네 압박이 좁은 구간에 중첩**. 반면 성장 자체는 빠름(이상적 포식 시 45→160이 6.5초) → **문제는 성장률이 아니라 "포위당해 못 먹는 것"**.
- 수정 3종: ① **위협 램프를 계단→연속**(`bhProg`=질량 로그 진행도)으로 바꾸고 상한 0.30→**0.22** — 티어 경계 절벽 제거. ② **그레이즈=서지 충전**(near-miss 시 +0.22) — 포위될수록 탈출 수단이 차오름, 위험이 자원이 되는 구조(nova-surge에서 검증된 훅). ③ 에딩턴 감쇠 시작 **60→85**(45→160 등반이 감쇠와 정면충돌하지 않게).
- 실측 개선: 위협 mass50 **19%→16%**·mass400 **23%→18%**(절벽 10→19 → 완만 11→16), eff mass100 **0.5→0.75**, 160 도달 6.5→**5.5초**.
- 검증 신규 midgame **11/11**(초반 완만·절벽 없음·후반 상한·위협은 여전히 증가·그레이즈 충전·근접링 비살상·감쇠 유지·등반 가능) + 전 회귀 **115/115**. `verify-spawn`은 라이벌 비중 감소로 단발 표본이 흔들려 3회 리필 합산으로 안정화(3연속 통과 확인).


## 2026-07-25 · 오너 논리 버그 2건 — "암석이 블랙홀을 먹음"·"형태 깜빡임"(1.2)
- **① 암석/별이 블랙홀을 삼킴**: 라이벌 스폰이 `S.mass*rnd(0.7,1.9+)`라 **30%가 나보다 작게** 생성 → `edible` 판정 → AI가 도망 → 미행성(mass 2.3)이 블랙홀을 포식. **물리적으로 불가능**(블랙홀은 아무리 작아도 반대로 삼킴). → **비-블랙홀 형태(암석·별)에겐 라이벌이 질량 무관 항상 치명**, AI도 항상 추격. 내가 블랙홀이 되면 질량 비교 복귀(공정한 싸움) = **진화 아크의 의미 강화**("블랙홀이 되어야 반격 가능"). 전부 치명이 되므로 등장 빈도 완화(rock 0.08→0.06·star 0.14→0.10).
- **② 형태가 변했다 되돌아옴**: `formOf(mass)`가 **양방향**이라 임계(3·12) 근처에서 침식으로 질량이 오르내리면 형태가 깜빡이고, `nf===2&&wasStar` 조건이 재충족돼 **초신성이 반복 폭발**. → **형태 단조 증가**(`Math.max(S.formIdx,formOf)`) — 블랙홀은 별로 되돌아가지 않는다는 물리적 사실과도 일치. 임계 왕복 14회 실측: 형태 최저 2 유지·충격파 0회 재발.
- 부수: **플레이어 암석이 소행성 먹이와 구분 불가**(회색 다면체로 동일) → pRock에 청백 코어 글로우 = "나" 식별.
- DEV 훅 `objInfo.edible`이 옛 공식을 써서 오탐 → 실제 판정과 동기(훅이 진실을 보고해야 함). `verify-spawn`은 필드를 안 비우고 측정해 새 스폰 2개만 보던 하네스 결함 수정.
- 검증 신규 forms-logic **10/10**(미행성/별에게 작은 BH 비-먹이·BH되면 먹이 복귀·접촉 사망·형태 무회귀·초신성 1회·전진 아크) + 전 회귀 **104/104**.


## 2026-07-25 · 오너 버그 3연쇄 — 무채색·성장폭주·점수미등록(1.1)
- 오너 실측 스샷: **PEAK 33,178,895,970 M☉ · SCORE 2,045,837,230,383 · "score not ranked"** + "어느 시점부터 전부 무채색이라 적을 식별·회피할 수 없다". 셋은 한 뿌리였음.
- **① 무채색(직접 원인) = 안개 포화**. `FogExp2(density 0.0016)` 고정인데 카메라 거리가 질량^⅓로 커짐 → 객체 거리도 커져 안개 계수 `1-exp(-(d·dist)²)`가 **100%로 포화** → 모든 객체가 안개색 단색. 실측: mass 1.2=0.5% · 5,000=10.9% · 330억=**100%**. → **안개 밀도를 카메라 거리에 반비례**(`0.047/camDist`)시켜 어느 규모에서도 0.5%로 일정. **의도된 연출 아님, 명백한 버그.**
- **② 성장 폭주(근본 원인)**: 먹이가 항상 내 질량의 12~82%라 한 입에 최대 +51% → **지수 폭발**(71초에 330억). → **에딩턴 한계** 도입(실제 천체물리 — 블랙홀은 자기 복사압 때문에 무한정 빨리 못 먹는다): `accEff=1/(1+max(0,m-60)/40)`. 진화 구간(~60)은 온전, 이후 감쇠 → **지수 → 선형**. 실측(위협 배제·초당 2회 포식): 10s 143 → 20s 413 → 35s 797 → 50s 1177 → **75s 1804**(이전 330억).
- **③ 점수 미등록**: 폭주로 score 2조 → Edge Function 상한 1e8 초과로 거부("score not ranked"). ②로 자연 해소(75초 198만, 제출 가능).
- 부수: 큰 수 표기 `fmtBig`(33178895970 → 33.00B, 1500 → 1.5K) HUD·게임오버 적용. 코덱스 16번째 **EDDINGTON LIMIT**(효율 0.5 미만 시 발견) — 버그 수정이 교육 콘텐츠가 됨. 스킨 임계 16.
- 검증 전 회귀 **95/95**(evolution 18·codex 19·nemesis 13·menu 17·p4 11·spawn 10·lategame 7). 스크린샷 전후 대조(fog-broken → fog-fixed: 무채색 덩어리 → 보라·크림·초록 행성과 라이벌 구분).
- **주의(오너 안내 필요)**: 성장 스케일이 바뀌어 기존 리더보드 기록(최고 26,924)과 새 점수(수십만~수백만)가 같은 보드에 섞임.


## 2026-07-25 · ★ 진화 아크 — 블랙홀을 "시작"에서 "도달"로 (1.0)
- 오너 아이디어("행성으로 시작해 레벨업하면 블랙홀") 채택. 논의 문서(Artifact) 제시 → **권장안대로 진행** 승인. 게임 최대의 빈 곳(진행 아크·변신 쾌감)을 실제 별의 일생으로 해결.
- **4단계 압축 아크**: `PLANETESIMAL`(mass<3) → `MAIN SEQUENCE`(<12) → **`SUPERNOVA`(전이 이벤트)** → `BLACK HOLE`. FORMS 테이블이 형태별 **기동성·포식반경·흡입권·렌징·유출률·라이벌비중**을 전부 다르게 잡아 "한 판 안에서 게임이 세 번 새로워짐".
- **비주얼 교체**: 미행성=회색 암석(Icosahedron·emissive) / 항성=자체발광 구체+코로나 2겹 / 블랙홀=기존(지평선·강착원반·광자링). `applyForm`이 파츠 visible 토글.
- **초신성 이벤트**(절정): 흰 화면 플래시 + 확장 충격파 링 + 화면흔들림 + **1.6s 무적** + 주변 일소(작은 것 흡수·점수 / 큰 것·라이벌 밀어냄) + 배너 + 코덱스 발견. 실측 +31 swept·+188점.
- **렌징이 보상이 됨**: 형태별 렌즈 배수 0.14 → 0.42 → **1.0** — 블랙홀이 되는 순간 시공간 왜곡이 확 켜진다.
- **질량 유출의 이유가 형태마다 다름**(전부 실제 현상): 침식 ERODING / 항성풍 STELLAR WIND / 호킹 EVAPORATING — HUD 매스바 라벨도 교체.
- **밸런스**: 존 5종(미행성 회청·항성 온기 + 블랙홀 3티어), TIERS 임계 상향(45/160), 네메시스는 **블랙홀 도달 이후만**, 라이벌 비중 형태 기반(초반 5% · 후반 상한 30% — 오너 지적 45% 과밀 해소). 코덱스 13→**15항목**(mainsequence·supernova), 스킨 임계 7/15.
- 검증 진화 **18/18**(형태 전환·비주얼·렌징 스위치·초신성 5종·중복방지·네메시스 게이팅·위협밀도·회귀) + 전 회귀 갱신 후 **78/78**(codex 19·nemesis 13·menu 17·p4 11·spawn 10·lategame 7 + evolution 18). 스크린샷 4단계 실측.


## 2026-07-25 · 오너 버그 재발 — 고질량서 화면이 회색화(0.9.1, 진짜 원인)
- 오너 "몸집이 커지면 행성·블랙홀 색이 회색으로 변한다"(0.7.1 emissive 수정 후에도 지속). **실측 분포로 진짜 원인 확정**: 타입 임계값이 **절대질량**(`mass<0.5 rock / <7 planet / else star`)인데 스폰 질량은 **플레이어 상대**(`S.mass*rnd(.12,.82)`) → 플레이어가 커지면 모든 먹이가 7 초과라 **전부 star**로 스폰. 측정: mass 1.2=planet 44%·rock 26% → **mass 60+ = planet 0%·rock 0%**, star 37~43% + neutron/pulsar(창백) + rival(검정) → 화면에 컬러가 사라짐.
- 수정: 타입을 **상대 크기 기준**으로 선택(`rel<0.30 rock / <0.62 planet / else star`) → 어느 규모에서도 컬러 먹이 유지. 실측 mass 400에서 rock 11%·planet 20%(전 0%). + **별 색 다양화**: 실제 항성 분류 6색(O/B 청백 → A/F 백색 → G 노랑 → K 주황 → M 적색)로 별도 알록달록 + 교육 연결.
- 회귀 spawn 10/10·lategame 7/7·codex 19/19·menu 17/17. 스크린샷 실측(gray-fixed: 초대질량서 보라·초록·주황·핑크 복원).
- 미해결 관찰: 고질량서 **rival 비중 38~45%**(화면 다수가 검은 홀) — 위협은 유지하되 시각 균형 조정 여부는 오너 논의 항목.


## 2026-07-25 · 오너 피드백 — 메인화면 단조로움 → 시네마틱 히어로 리디자인(0.9)
- 오너 "메인화면이 너무 단조롭진 않나요?"(1920px 실측 스샷). **진단 4가지**: ① **HUD 누출**(#hud·#massbar·#right가 메뉴에서도 표시 → 좌상단 질량·우상단 SCORE가 비쳐 미완성처럼 보임) ② **블랙홀이 UI 침범**(정중앙 렌더 → PILOT 입력칸과 겹침) ③ **텍스트 리본**(.msg max-width:30ch → 6줄로 쪼개진 얇은 기둥) ④ **평면 구조**(전부 같은 폭·무게 세로 나열, 좌우 70% 죽은 공간).
- **시안 2종 제시**(scratchpad/accretion-menu-mockup.html) — A 시네마틱(비대칭 히어로) / B 콘솔(3패널). **오너 A 선택.**
- **구현(A)**: 좌측 UI 컬럼(태그라인→타이틀→훅 2줄→PILOT→CTA 2개 가로→링크+조작키 pill) + **우측 블랙홀 히어로**(메뉴 전용 카메라: camDist×0.56로 당기고 x축 -shift로 밀어 홀을 화면 우측 x≈0.77에) + **하단 성과 스트립 4카드**(코덱스 진행바·개인최고·글로벌랭크·오늘 모드) = 리텐션 훅 상시 노출. 좌→우 페이드 오버레이(홀 선명·텍스트 가독). 메뉴에서도 강착원반 회전(생동감).
- **영구 스탯**: `acc_best`(게임오버 갱신)·`acc_rank`(글로벌 제출 성공 시) localStorage.
- **반응형**: 860px 이하 = 중앙정렬 회귀 + 홀을 배경으로 물림(camDist×1.25)·오버레이 진하게·스트립 숨김(원래 겹침 문제 재발 방지). 높이 520px 이하(모바일 가로) = msg 숨김·간격 압축으로 넘침 방지.
- 검증 메뉴 **17/17**(HUD 게이팅 3종·스트립 4값·홀 우측 x=0.77·좁은화면 중앙회귀·스트립숨김·모바일가로 뷰포트 적합·에러0) + **전 회귀 60/60**(p4 11·lategame 7·spawn 10·codex 19·nemesis 13). 스크린샷 실측(menu-A-desktop/playing/narrow/mobile).


## 2026-07-25 · 중독성 P#3 — 데일리 챌린지(모디파이어 + 데일리 리더보드)(0.8)
- 런간 리텐션 핵심: **날짜별 모디파이어(그날은 모두 같은 규칙) + 데일리 리더보드** → "매일 새 도전으로 다시 옴". (완전 시드 결정론 대신 모디파이어 방식 — 저위험·shared challenge.)
- **백엔드**: accretion_runs에 `day`·`modifier` 컬럼 + (day,score) 부분인덱스. Edge Function v2 = day(YYYY-MM-DD 검증)·modifier 저장 + **범위별 순위**(데일리=그날 안·글로벌=day null 일반런). LB.top→글로벌(day is null), LB.topDaily(day)→그날.
- **모디파이어 5종**(dayIndex%5로 결정): SWARM(밀도1.5)·DARK FOREST(라이벌1.7)·FAMINE(증발1.7)·HUNTED(에이펙스 처음부터)·FRENZY(콤보창 2.4s·점수1.3×). begin에서 S.mod* 리셋 후 데일리면 fx 적용 + 안내 배너.
- **UI**: 시작화면 "◆ DAILY · {오늘모드}" 버튼 + 📅 DAILY BOARD 링크. 게임오버 순위 "DAILY · MOD RANK #N", 보드는 방금 모드 기준(overBoard 컨텍스트). AGAIN=같은 모드 재시도.
- 검증: 백엔드 5종(curl: 글로벌/데일리 제출·day검증·보드분리) + 헤드리스 **16/16**(모디파이어 5종 효과·HUNTED 즉시추격·normal 리셋·데일리 제출/보드/글로벌분리/모디파이어태그·회귀·에러0). 스크린샷(daily-start). 테스트행 삭제(실플레이어 4런 보존 — 리더보드 실사용 중).
- 다음: #4(루프 깊이 — 서지/근접회피 전술화). 이후 온보딩·출하.


## 2026-07-25 · 오너 버그 — 고질량서 행성 색 세탁(0.7.1)
- 오너 "뒤로 갈수록 모든 행성 색이 무채색화". 원인: 행성·암석만 `MeshStandardMaterial`(조명받음)인데 티어 존 hemiLight가 초대질량서 **금색으로 shift** → 조명받는 행성이 금색-단색으로 물듦(별·중성자 등 Basic은 무관). fog는 밀도 0.0016로 비원인(계산 확인).
- 수정: 행성/암석 재질에 **자기 색 emissive**(planet 0.42·rock 0.18) → 존 조명(금색)에도 고유색 유지. 존 분위기(안개·비네트·렌징)는 그대로. 실측 전후 대조(wash-high vs -fixed: 크림 세탁 → 보라·초록·핑크·청록 복원).
- 회귀 7/7·10/10.

## 2026-07-25 · 중독성 P#2 — 네메시스(에이펙스 포식자) + 클라이맥스(0.7)
- "밀어붙일 이유·후반 긴장·클라이맥스"를 한 번에. **네메시스** = INTERMEDIATE 티어부터 등장하는 항상 더 큰 명명된 블랙홀. 라이벌 시스템 재활용.
- **역학**: 질량 = 내 1.35× **지연추종**(lerp k0.35) → 급성장하면 잠깐 추월 가능. 크면 **집요 추격(1.35×속도)=공포**, 내가 커지면 **도망(0.75×)=취약**. 컬링 예외(끝까지 쫓음). HUD `☠ APEX ▸ 거리`(추격=붉음 펄스 / `★ VULNERABLE — CONSUME`=초록).
- **클라이맥스**: 되먹으면 `★ APEX CONSUMED` 세리머니 + 큰 점수(질량×45)·화면흔들림·SFX + **QUASAR 코덱스 발견**(13번째 항목) + `apexKills++` → **2.8s 뒤 더 큰 에이펙스 등장**(무한 에스컬레이션 목표). 잡히면 사망.
- 코덱스 12→**13항목**(quasar=클라이맥스 보상). nemPending은 **게임시간(S.t)** 기반(일시정지·헤드리스 정확).
- 검증 네메시스 **13/13**(초반無·INTERMEDIATE 등장·추격 거리감소·급성장→취약·되먹기 apexKill++/+826/QUASAR·더큰 재등장 58 vs 18·치명·리셋) + 코덱스 회귀 19/19 + 후반 회귀 7/7. 스크린샷(nemesis-hunt: APEX▸34 경보).
- 다음: #3(런간 통화·데일리 챌린지) → #4(루프 깊이).

## 2026-07-25 · 중독성 P#1 — 코덱스 수집 + 스킨 언락(0.6)
- 오너 "냉정하게 재미 충분? 후킹/중독성?" → 냉정 평가: 6/10, "기분 좋은 프로토+정체성"이나 후킹·리텐션 얇음(코어 루프 얇음·목표/클라이맥스 없음·**메타 프로그레션 0**·교육은 차별점이지 중독동인 아님). 처방 1~4 중 **#1(코덱스를 수집목표+메타언락으로 승격)** 부터 착수 — 교육 차별점을 그대로 리텐션 엔진으로.
- **코덱스 확장·영구화**: 6종 천체 → **12항목**(+ surge/tidal/intermediate/supermassive/evaporate/devoured = 플레이 이벤트로 발견, 실제 과학 팩트). `localStorage acc_codex`로 **런 넘어 영구 수집**. 발견 토스트에 N/12 진행 표시.
- **수집화면**(시작·게임오버서 진입): 12항목 그리드(발견=팩트·미발견=??? LOCKED)·N/12 진행·설명. 시작화면 버튼에 진행도 상시 표시.
- **스킨 언락**(수집=보상): 강착원반 팔레트 3종 — VOID(기본)·SOLAR(6/12)·AURORA(12/12). 수집률로 언락, 수집화면서 선택, `applySkin`이 원반 vertexColor+광자링 리페인트. 6/12 도달 시 "SKIN UNLOCKED" 배너.
- 이벤트 트리거: 서지 발동→gravity assist·티어업→intermediate/supermassive·근접회피→tidal·증발/피식→hawking/event-horizon. (`SEEN`→`CODEX` 전환, DEV 훅 seen 수리.)
- 검증 헤드리스 **19/19**(리셋·발견·6→SOLAR언락/AURORA잠김·스킨선택·**영구저장(리로드)**·수집화면 12항목 6발견6잠김·이벤트4종 발견·성장회귀·에러0). 스크린샷 실측(codex-screen·skin-solar).
- 다음: 처방 #2(목표/클라이맥스·네메시스) → #3(런간 통화·데일리) → #4(루프 깊이).

## 2026-07-24 · 오너 피드백 — 후반 이동정지·무료함(0.5, 후반 오버홀)
- 오너 "커질수록 움직임이 느려지다 멈추는 느낌 + 어느정도 커지면 무서운 게 없어져 무료".
- 진단: ① `maxSpd=22/(1+mass*0.010)` → 크기 반비례로 감속 + 카메라 줌아웃 = 이중 감속("멈춤"). ② 스폰이 `+mass*1.5`라 고질량에 226+유닛 밖 = 화면 공백. ③ 원거리 컬링 없음 = 필드 안 갱신. ④ 라이벌 속도 고정(4.6)·비중 고정(18%) = 커지면 못 쫓아와 시시.
- 수정: ① **maxSpd=16+rad(mass)·5**(크기 비례↑, 줌아웃 보정 → 커져도 이동감 유지). ② 스폰을 **뷰(camDist) 바로 밖**(camDist·1.15~1.9)으로 → 고질량도 화면 근처 밀집. ③ **원거리 컬링**(camDist·2.8 밖 재활용) → 필드 신선·위협 즉응. ④ 라이벌 속도 **크기 비례**(rs=1+rad·0.6) + 티어↑ 시 비중 **0.18→0.42**·크기 최대 **2.8×**.
- 검증 7/7: 이동 큰질량 **1.9× 더 멀리**(정지 해소)·위협 공유 **20%→42%**·대형라이벌 79% 치명·초반 여전히 먹이 18%·성장 회귀·에러0. 스크린샷 실측(late-supermassive: 고질량 밀집 필드).
- 다음: 후반 클라이맥스(목표/승리조건 or 무한 에스컬레이션) 방향 오너 확인.

## 2026-07-23 · 리포 통합 — 단일 public `sh-binx/accretion`
- 오너 결정: 소스+데모 2리포 분리는 "위키 감추기"가 유일 이유였는데 위키에 민감정보 없음(스캔 확인) → **하나로 통합·전부 public**.
- **구조 변경**: `src/index.html`·`src/three.min.js` → **repo root**(Pages가 root 직접 서빙). `src/` 제거. serve.sh 기본 dir `src`→`.`, PORTS/README 갱신, 루트 스크린샷 `/*.png` gitignore.
- **배포 모델**: 이제 **`git push`만** = 배포(별도 accretion-demo 복사 단계 폐지). DEV 훅 `?dev=1` 프로덕션에 무해(게이트됨).
- **새 리포**: github.com/sh-binx/accretion (public, 전체 이력 푸시). **라이브 = https://sh-binx.github.io/accretion/** (URL이 /accretion-demo/ → /accretion/ 로 변경).
- accretion-demo 리포는 은퇴(신규 URL 검증 후 정리). 검증: root 서빙 스모크(200·three.min.js·credit)·헤드리스 P4 11/11.

## 2026-07-23 · 오너 피드백 — "운석은 먹는데 행성은 못 먹음"(0.4.2, 스폰 재설계)
- 원인(분포 분석, S.mass=1.2): 식량 52%는 전부 질량<1.2라 **전부 'rock'**(먹힘). 'planet'으로 렌더되는 20% 버킷은 질량 1.08~1.8 → **87%가 내 질량보다 커서 못 먹고, 라이벌도 아니라 위협도 아닌 "장식 행성"**. 컨셉("행성 먹어 성장·더 큰 블랙홀 회피")과 어긋남.
- 수정: **비-라이벌은 전부 먹이**로 재설계 — `food(66%) rock/planet/star 크기별(항상 질량<나) · dense(16%) neutron/pulsar · rival(18%)=유일 위협축`. rock/planet 임계값 1.2→0.5로 낮춰 중간 크기 먹이가 'planet'으로 렌더. 성장 진행: 저질량=암석+행성 → 중간=행성 → 고질량=별.
- 검증 10/10: 초반 행성 8개·중반 11개 **전부 edible**·비-라이벌 100% 먹이·라이벌 위협 유지·심은 행성 near-miss 포식·성장 회귀·에러0. objInfo 실측(35/35 edible, 행성 9).

## 2026-07-23 · 오너 피드백 — 먹기 판정 빡빡함(0.4.1)
- 오너 "너무 정확히 들어맞아야 행성을 먹는다". 원인(수치 분석): 중력 흡입이 **`s²` falloff**이라 가장자리에서 거의 0 → 플레이어가 빠르게 스치면 흡입이 못 따라잡아 near-miss 유실. 먹기 반경도 `mouth+o.r*0.4`로 빡빡.
- 수정: ① 흡입 **선형 falloff(`s`)** + 반경 `mouth*3.2→3.8` + 세기 `(14+hr*5)→(22+hr*8)`, ② **중력권 진입 시 표류 속도 감쇠**(스쳐도 빠져나가지 못하고 포획), ③ 먹기 반경 `o.r*0.4→0.75`·mouth `1.7→1.9`.
- 검증(통제 near-miss, 이동 홀 스윕): offset **1.5·3.0·5.0 전부 포획**·offset 14는 미포획(화면 진공청소기 아님)·성장 회귀·에러0 = **7/7**. P4 회귀 11/11 유지.

## 2026-07-23 · P4 웰메이드 — 서지(중력 슬링샷) + 티어별 비주얼 존
- **서지 능력**(에너지 기반 대시): 먹으면 에너지 충전(+0.09/eat), **만충 시 발동**(Space/게이지 탭). 0.9s간 속도 2.3×·포식 반경 1.6× → 사냥·회피 양용. 상승 휘익 SFX·"⚡ SLINGSHOT" 배너·중력 어시스트(실제 스윙바이) 교육 프레이밍. HUD 하단 충전 게이지(만충 시 금색 펄스 + PC "SPACE"/모바일 "TAP" 힌트).
- **티어별 비주얼 존**: 성장 티어에 따라 환경 팔레트 전환 — STELLAR=청·INTERMEDIATE=보라·SUPERMASSIVE=금. 안개색·반구광·**렌징 강도(1.0→1.4)**를 매 프레임 목표로 lerp + 전면 비네트 워시(CSS box-shadow 1.6s 전환). 성장이 크기뿐 아니라 환경 전체로 체감.
- 입력 가드 재활용(타이핑 중 Space 무시)·모바일 게이지 탭은 캔버스 아니라 통과.
- 검증 헤드리스 **11/11**(게이지 표시·충전 READY·미충전 게이팅·발동 소비·**가속 실측 8.5→19.6=2.3×**·티어 도달·안개색 전환·렌징 강화·성장 회귀·에러0). 스크린샷 실측(p4-violet/p4-gold — 보라·금 존 뚜렷).
- 다음: P5(온보딩·데일리 실제 천체 챌린지·밸런스 튜닝·출하). 서지 손맛·존 전환은 오너 실플레이 피드백 대기.

## 2026-07-23 · P3 웰메이드 — 글로벌 리더보드(Supabase·안티치트)
- 오너 "쭉진행" → 로드맵 P3 착수. **점수 정립 + 글로벌 리더보드**.
- **백엔드**(nova-surge Supabase `znboesjyunfkfqepsumu` 재활용): 새 테이블 `accretion_runs`(name/score/mass/tier/dur, RLS **공개 읽기만**) + Edge Function `accretion-submit`(verify_jwt=false, **서비스롤 삽입** + 안티치트: 즉시고득점(score>5k&dur<3)·비현실레이트(score/dur>12k) 거부, 이름 새니타이즈·XSS 이스케이프). 클라 직접삽입은 RLS 42501로 차단.
- **프론트**: `LB` 모듈(제출/읽기)·파일럿 이름 입력(localStorage `acc_name` 지속)·게임오버 **순위 리빌**(GLOBAL RANK #N)·보드 오버레이(TOP 100, 메달·본인 하이라이트·PLAY/CLOSE)·시작화면 🏆 링크. 제출 실패는 **네트워크오프라인 vs 서버거부** 구분 메시지.
- **입력 가드**: 이름 입력 중 게임키/뮤트 오작동 방지(`typing()` 가드) · 모바일 조이스틱은 플레이 중 캔버스 터치에서만(버튼 탭 보존).
- 검증: 백엔드 4종(curl: 제출·치트거부·RLS차단·읽기) + 헤드리스 **14/14**(이름지속·제출·안티치트·보드렌더·XSS이스케이프·순위리빌·타이핑가드·버튼·에러0). 스크린샷 실측(lb-board/lb-rank). 테스트행 전량 삭제(출시 클린).
- 다음: P3b 데일리 실제 천체 챌린지(시드 결정론) 또는 P4(능력/파워업·티어별 존 비주얼·온보딩).

## 2026-07-23 · P2 웰메이드 — 콘텐츠 다양성 + 라이벌 AI + 과학 팩트(코덱스)
- **신규 천체**: 중성자별(작지만 초고밀도 청백광)·펄서(회전 빔). 밀집 천체는 시각적으로 작게(0.72x)라 "밀도" 체감(먹기 판정은 질량 기준).
- **라이벌 블랙홀 AI**: 크면 추격(홀 쪽 가속)·작으면 도망(반대) — 위협/식량이 동적. (기존엔 표류만.)
- **코덱스**: 천체 처음 먹거나(rival은 근접회피) 만나면 **실제 과학 한 줄**(하단 토스트) + SEEN 기록. 팩트 6종(중성자별=각설탕 10억톤·펄서=초당700회전·별=초당6억톤 등). 교육 차별점 본격화.
- 검증 6/6(신규천체·AI·코덱스 seen/토스트·성장·에러0). 스크린샷 실측(p2-codex.png).
- 다음: P3(점수 정립 + 글로벌 리더보드 Supabase + 데일리) — 오너 피드백 후 진행 예정.

## 2026-07-23 · P1 웰메이드 — 사운드 + 주스 (로드맵 1단계)
- 오너 "이대로 고도화·웰메이드로" → 로드맵 8축×5단계 수립(scratchpad/accretion-roadmap.html). P1 착수.
- **오디오**: WebAudio 확장 — 우주 앰비언트 드론(저음 3오실+LFO 필터)·먹기(콤보 피치↑)·티어업 팡파레·위험 경고·근접회피·증발/사망·UI. master gain + **뮤트 토글**(🔊 버튼·M키).
- **주스**: **콤보**(1.4s 내 연속 먹기 배수, HUD ×N 중앙 표시)·**점수**(질량×콤보 배수, 티어업 보너스, HUD)·**강착 파티클**(먹을 때 스파클이 홀로 나선 흡입)·**근접 회피 보너스**(큰 블랙홀 스치면 CLOSE!+점수)·화면 흔들림 콤보 연동.
- 게임오버에 SCORE·MAX COMBO 표시. 검증 8/8(앰비언트·점수·콤보·파티클·뮤트·티어업·HUD·에러0).
- 다음: P2 콘텐츠 다양성(천체 종류·라이벌 AI·초신성)+교육(과학 팩트·코덱스).

## 2026-07-23 · 오너 피드백 2건 — WASD(IME 버그)·먹기 난이도
- **WASD 안 됨(화살표만 됨)**: 원인 = **한글 IME**. `e.key`로 키를 읽어 한글 모드에서 W/A/S/D가 ㅈ/ㅁ/ㄴ/ㅇ로 들어와 매칭 실패(화살표는 IME 무관이라 됐음). → **`e.code`(물리 키, 레이아웃·IME 무관)**로 전환: KeyW/KeyA/KeyS/KeyD. 검증: 전 키 steer 정확.
- **별이 잘 안 먹힘**: 판정 반경이 빡빡(정확히 겹쳐야). → **중력 흡입** 추가(edible 객체가 mouth*3.2 내면 홀로 끌려옴, 가까울수록 강). + mouth 1.55→1.7·판정 여유↑. 사실적(블랙홀 중력)+손맛+먹기 쉬움. 검증: 정지 홀 5거리 먹이 흡입되어 먹힘(3→3.58).

## 2026-07-23 · 오너 플레이 피드백 3건 수정 (룩·이동체감·성장체감)
- **블랙홀 룩**(행성 같다→): 토성형 밝은 토러스 제거. 검은 사건지평선 + **도플러 비대칭 강착원반**(vertexColors, 접근측 청백·후퇴측 적, 상대론적 부스팅) + **광자 고리**(그림자 감싸는 얇은 빌보드 링). 렌징 강화로 배경별이 아크로 휨. "진짜 블랙홀" 룩 확인.
- **이동 체감**(꾹 눌러도 안 나아감→): 카메라가 홀 중심 고정 + 별 스카이박스라 이동 무피드백이 원인. **월드 고정 시차 먼지**(460개, 홀 주위로 랩→무한 필드) 추가 → 이동 시 먼지 스트림. (이동 메커닉은 정상이었음 — steer 검증 완료.)
- **성장 체감**(질량↑인데 화면상 작아짐→): camDist가 질량 선형 줌아웃이라 화면상 홀이 오히려 축소됐음. **camDist=22+rad(mass)*7**(질량^⅓ 완만)로 → 먹을수록 화면에서 커짐. 성장 메커닉 확인: mass 1.2→5.72(미니블랙홀 시작→성장).
- 검증: 신규 룩 스크린샷(bh-small/grown)·성장 1.2→5.72·JS에러 0. 재배포.

## 2026-07-23 · 컨트롤 추가 — 방향키/조이스틱 (오너 제안)
- 오너 제안 반영: 관성 블랙홀은 "밀어서 조종"이 무게감이 살아 방향입력이 더 맞음. **공존 구현**(우선순위 keys > joystick > mouse):
  - PC: **WASD·화살표**(방향 가속) + 마우스 폴백(특정 먹이 정조준)
  - 모바일: **플로팅 조이스틱**(터치 지점에 생성, 드래그로 방향+세기)
- `steer()` 단일 리졸버로 입력 통합 → 관성 이동(desired velocity lerp, 놓으면 감속). 검증: 4방향+화살표 steer 벡터 정확·조이스틱 우상 드래그 {x:.67,z:-.74,mag:1}·마우스 폴백·회귀(성장/축소/증발) 전부 통과, JS에러 0. (헤드리스 첫 키 유실은 테스트 아티팩트 — steer가 진실.)
- 재배포: sh-binx/accretion-demo → https://sh-binx.github.io/accretion-demo/

## 2026-07-23 · 모바일 가로모드 게이트 (오너 지적)
- 모바일 세로 감지(pointer:coarse + innerHeight>innerWidth) → **회전 안내 오버레이 + 일시정지**(orientPaused → update 프리즈, 증발 안 됨). 가로면 자동 해제·정상 플레이. 데스크톱 무영향. nova-surge와 동일 정책(가로 전면). 7/7 검증.

## 2026-07-23 · 프로토 v0.1 — 코어 루프 동작 (10/10 검증)
- `src/index.html` — Three.js r128 단일 파일. 관성 활공·먹고 성장(질량^⅓ 반경)·**호킹 축소 타이머**(작을수록 빠름)·**더 큰 블랙홀에 먹힘**(조석 파괴)·티어(항성/중간/초대질량)·카메라 줌아웃.
- **중력 렌징 포스트프로세스**: 배경별 전용 RT → 프래그먼트 셰이더(1/r 편향 + 1/r² 확대 + 광자 고리 + 블루시프트). 질량 따라 강도↑. **배경별 휘어짐이 실제로 보임 = 시그니처(2D 불가) 확인.**
- 검증 `scripts/verify-proto.mjs` 10/10: 부트·성장·호킹축소·렌징강도·티어·증발게임오버·재시작·JS에러0. 스크린샷 실측(proto-lens-mid.png).
- 서버: `sh scripts/serve.sh src` → http://localhost:3040. three.min.js는 nova-surge slice.built에서 추출(r128, 589KB).
- 다음: **오너 손맛 판정**(축소압력·먹는 리듬 재밌나) → 튜닝. 이후 콘텐츠(데일리·코덱스·사운드)·배포.

## 2026-07-23 · 프로젝트 부트스트랩 + 컨셉 v1
- nova-surge(3D 슈터)가 CrazyGames에서 "차별화 부족"으로 거절 → 차기작으로 피벗. 오너 아이디어(우주·블랙홀 성장·교육) 기반.
- 경쟁 조사([research/competitive-2026-07-23.md](research/competitive-2026-07-23.md)): "움직이며 먹고 성장"(Holey.io/Osmos/Solar 2/Tasty Planet)은 포화. 단 **"과학이 곧 재미"인 아케이드**는 빈 곳. 오너 관찰로 정정: CG 직접 경쟁은 Holey.io 중심(Yumy 라이브 미확인·Black Hole Idle은 클리커).
- 결정 방향: 검증된 손맛(움직이며 먹기)은 유지 + **실제 물리를 규칙으로** 차별화(호킹 축소타이머·질량 위계·조석 파괴) + **3D 중력 렌징**(2D 클론 불가). 상세 [design/concept.md](design/concept.md).
- 스캐폴드: 포트 3040–3044, LLM 위키, AGENTS.md(게임 검증 규약 재활용), serve.sh. project-bootstrap 표준.
- 다음: 오너 컨셉 리뷰 → 코어 루프 프로토(움직임+먹기+호킹 축소+렌징 1샷)로 손맛 검증.
